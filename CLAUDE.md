# gauntlet

エージェントが書いたコードを、人間がコードを読まずに機械的な制約で縛る、チーム共用の品質ゲート。
coverage・CRAP・mutation testing・重複（+ 対象リポジトリの型チェック）を `quick`（差分に閉じた検査。エージェントの `git commit` を PreToolUse フックで止める）と `full`（全量 + ラチェット + mutation。CI）の 2 tier で回す。

## 設計を貫く 2 つの語

新しい判断を下すときは、まずこの 2 語に当てる。

**flaky** — 走ったり走らなかったりするゲート、環境によって答えが変わるゲートは、数回の食い違いで無視されるようになる。緑は常に同じ意味でなければならない。

**gameable** — エージェントが自分の書いたテストで自分を採点する構造では、通すための最短経路が「品質を上げる」ではなく「測定をすり抜ける」になる。最短経路を塞ぐ。

## ドキュメント

- **[DESIGN.md](DESIGN.md)** — 設計判断とその理由、検討して外したもの、承知の上で残るリスク。
  閾値・tier・差分の起点・ratchet・配布形態を変えるとき、「なぜ X が無いのか」を問うときに読む。
- **[PLAN.md](PLAN.md)** — 実装の順序、各ステップの完了条件、実測済みのベースライン、前提が崩れたときに戻る先。
  次に何を作るか決めるとき、リポジトリの実測値が要るときに読む。

## 足す前に

**YAGNI。** 新しいファイル・設定項目・抽象は、今すぐ読む呼び出し元があるときだけ足す。
ひとつの性質を守る仕組みは 1 つに保つ。2 つ目を足す前に、1 つ目が破れた証拠を出す。
速さと正しさは見込みで作り込まず、先に測る。

## ドキュメントを事実と一致させる

DESIGN.md / PLAN.md / CLAUDE.md は、コードと実測の現状に一致していなければならない。
実装や計測でここに書かれた事実が変わったら、**同じターンのうちに**該当箇所を直す。

- 閾値・バージョン・採用したライブラリ・スコープを変えた → DESIGN.md
- ステップの完了条件を満たした / 前提が崩れた / 新しい実測値を得た → PLAN.md
- 新しい判断基準を導入した → CLAUDE.md

**進捗の正は PLAN.md。** セッション内のタスクリストはその写しであって、正ではない。

事実が変わっていないターンでは何もしない。「一応更新しておく」はしない。

## このリポジトリ自身について

gauntlet は自分自身の最初のユーザーで、閾値も ratchet も自分に適用する。
CRAP ≤ 8 の一次情報は dogfooding で出た: 集中開発（0.18〜0.26）で絶対閾値に 9 回止められ、
全て関数分割か純関数の抽出で解決した（DESIGN §7-4）。閾値を緩める理由はまだ出ていない。
厳しすぎて実装が歪むと判断したら、閾値ではなく dogfooding の方を外してよい。

**main へは push できない。** ruleset で塞いである（PR 必須・`gauntlet` が
required status check・bypass なし）。変更はブランチ → PR → CI 緑 → マージ。
push の回の `full` は事後の通知で、ゲートとして働けるのは PR の回だけだから（PLAN の
「main を ruleset で守る」）。
