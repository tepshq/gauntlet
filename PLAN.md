# 実装計画

[DESIGN.md](DESIGN.md) を実装する順序。上から順に進める。
各ステップは「前のステップが動いていることを前提に、次の判断材料を得る」ように並べてある。

閾値・tier・差分の起点といった確定値は [DESIGN.md](DESIGN.md) にあり、ここには再掲しない。

## 実測済みのベースライン（2026-08-07）

設計判断の根拠になった数値。再取得には各リポジトリでテストスイートを回す必要があるので残す。

| repo | test files | tests | src LOC | 実測 |
| --- | --- | --- | --- | --- |
| hue | 20 | 412 | 4.6k | `vitest run --coverage` **4.96s** / stmt 87.8% branch 81.4% func 78.5% |
| teps | 183 | 2222 | 31k | `vitest run` **7.89s**（coverage provider 未導入）/ `tsc --noEmit` **1.65s** |
| duct | 397 | — | 60k | 依存が未 install（`typescript` も `vitest` も無い） |
| qm | 530 | — | 123k | `node --test`・vitest 無し → v1 対象外 |
| ccaf | 4 | — | 3.3k | workspaces → v1 対象外 |

teps の `vitest run` は real 7.89s に対し user 49.96s で、既にコアを飽和させている。
並列化による短縮の余地は小さい。

### `oxc-parser` の実測（リポジトリ全体、`turn` は変更ファイルのみなので上限値）

| repo | files | size | parse | walk | parse errors |
| --- | --- | --- | --- | --- | --- |
| hue | 45 | 262 KiB | 37ms | 9ms | **0** |
| teps | 372 | 2.9 MiB | 187ms | 43ms | **0** |
| qm | 1049 | 9.4 MiB | 712ms | 137ms | **0** |

対象 3 本は TypeScript 5.5 / 5.8 / 5.9 とバラバラだが、全て 0 エラーで読めた。
全関数が位置情報を持つ（qm: 22,817 / 22,817）。名前が直接取れるのは 2〜3 割。

### 部分実行 coverage の完全性（hue、`--changed HEAD~5`）

マージ方式を捨てた根拠。

| | full | partial |
| --- | --- | --- |
| 実行時間 | 4.96s | **4.88s**（節約ほぼ無し） |
| 変更された 4 ファイル | 20/20, 214/238, 17/17, 213/263 | **全て完全一致** |
| 変更されていない 17 ファイル | 計測あり | **全て欠落**（過少は 0 件） |

`--changed` は変更ファイルを import する全テストをモジュールグラフから選ぶので、変更ファイルの coverage は部分実行で完全になる。
なお hue は vitest 側に coverage 閾値（lines 89% / functions 80% / statements 89% / branches 82%）を設定しており、部分実行がそれに引っかかって落ちた。gauntlet は実行時にこれを無効化する必要がある。

### hue に対する CRAP の実測（閾値 8）

| 項目 | 値 |
| --- | --- |
| 対象関数 | 406（解析 23ms） |
| CRAP > 8 | **1 件**（0.2%） |
| 網羅率 0 の関数 | 71 |
| CC 分布 | 1:240, 2:54, 3:49, 4:23, 5:23, 6+:17 |
| 最も複雑な関数 | CC=8・網羅率 100% → CRAP ちょうど 8 で通過 |

`coverage-final.json` 全体の stmt は 1392/1557 = 89.40% で、vitest 報告の covered 数と一致する（割り当ての検算）。
よく整備されたリポジトリに対して閾値 8 はほとんど赤を出さない。ただし CC=8 の関数は判定点があと 1 つで落ちる。

---

## ステップ

進捗の正はここ。各ステップの見出しに状態を書く。

### 1. リポジトリ初期化と dogfooding の土台 — 完了

`package.json` / `tsconfig.json` / `vitest.config.ts`、および自分自身用の `gauntlet.config.json`。

TypeScript 7 要求の一次検証を兼ねる。ここで詰まるなら、パイロット投入前に設計へ戻れる。

**完了条件** — `npm install` が通り、`npx tsc --noEmit` と `npx vitest run` が両方緑。
`node_modules/typescript` の実バージョンが 7 系であることを確認済み。

TypeScript 7.0.2 で通った。ただし TS 7 の compiler API が無い件が判明し、パーサを `oxc-parser` に切り離した（DESIGN §2）。

### 2. 2 枚の抽象を先に固定する — 完了

共用すべきものは契約であって機構なので、先にここを書く。

- **アダプタが吐く JSON のスキーマ** — 関数単位の `{ file, name, line, cc, coverage, crap }` と実行メタ情報
- **tier の契約** — `turn` / `pr` がそれぞれ何を実行し、何を返し、どの exit code を出すか
- `gauntlet.config.json` の JSON Schema と起動時検証

**完了条件** — 不正な `gauntlet.config.json` を渡すと非ゼロで落ち、それを固定するテストがある。
レポート JSON の型が 1 か所に定義され、`turn` と `pr` が同じ型を返す。

実装で決まったこと:

- **config は「リポジトリの事実」だけを持つ。** 閾値やポリシーは全社共通なので入らない。`additionalProperties: false` にして、書いたつもりの設定が黙って無視されるのを防ぐ。
- **CRAP の式は core が持つ。** アダプタは cc と coverage を報告するだけ。2 言語目で式が 2 つになるのを防ぐ。
- **`CheckResult` に `skip` が無い。** 走らなかったチェックがあると緑の意味が実行ごとに変わる。走れないなら fail。
- **exit code は 0 と 2 のみ。** 違反も内部エラーも 2。`Stop` フックは exit 2 だけを阻止として扱い、それ以外を素通しするため、走れなかった gauntlet が緑になってはいけない。区別はメッセージで伝える。

### 3. CRAP の計算 — 完了

- 関数単位の cyclomatic complexity（AST）
- Istanbul coverage JSON から関数単位の coverage を引く
- `vitest --changed <merge-base> --coverage` の実行と、プロジェクト側 coverage 閾値の無効化

**キャッシュとマージは実測の結果、捨てた**（上の実測表と DESIGN §2）。部分実行の coverage は変更ファイルについて完全で、マージすると加算により偽の緑を作る。

**完了条件** — hue の変更ファイルについて、部分実行から出した CRAP がフル実行から出した CRAP と一致する。
テストが 1 つも無い変更ファイルが網羅率 0 として落ちる。
CRAP を超えた関数が、名前を持たないアロー関数であっても一意に名指しされている。

hue に対する通しの実測（起点 `HEAD~5`）:

| 項目 | 値 |
| --- | --- |
| 変更ファイル | 7（うち src の非テスト 4） |
| テスト | 412 件緑・**4.7 秒** |
| coverage に現れたファイル | 4（変更されたものだけ） |
| 触った関数 | 160 |
| CRAP > 8 | 1 件 — `src/server.ts:582 buildChannels > (anonymous)` CRAP 12.0 |

フル実行から出した違反と同一。152 関数を照合して不一致 0 件。
「テストが 1 つも無い変更ファイル」はユニットテストで固定してあり、通しでの確認は未実施。

**vitest の exit code は使わない。** プロジェクトが coverage 閾値を設定していると部分実行は必ず下回って exit 1 になり、しきい値の CLI 上書きは glob キー付き設定に効かない（hue で実測）。JSON reporter でテスト結果を、`coverage-final.json` で網羅率を別々に読む。これで設定の形に依存しなくなる。

### 4. ratchet と baseline ガード — 完了

- 触った関数 → 絶対閾値
- リポジトリ全体 → baseline 比較、悪化で fail
- baseline は改善時のみ自動更新、後退は明示フラグ
- `PreToolUse` で `Bash` と `Edit|Write`(baseline パス) を塞ぐ

**完了条件** — baseline を悪化させる変更が fail する。
エージェントとして baseline を書き換える 2 経路（`Bash` / `Edit`）が両方ブロックされることを、実際に試して確認済み。

実装で決まったこと:

- **差分は行単位で見る。** ファイル単位だと 1 行直しただけでそのファイルの全関数が絶対閾値の対象になり、既存リポジトリが即座に赤くなる。
- **未コミットの変更と新規ファイルを含める。** エージェントはコミットせずにターンを終えるので、コミット済みだけでは書いたばかりのコードが素通りする。
- **リポジトリ全体のラチェットは `pr` でだけ判定する。** 部分実行の coverage を全体に当てると偽陽性が出る（hue で 201 件）。
- **改善は gauntlet 自身が自動で baseline に固定する。** エージェントは baseline を触れないので、改善の記録は道具側の仕事になる。
- baseline の後退を git HEAD と比較して弾く仕組みは**作らなかった**。`PreToolUse` ガードで足りているうちは二重にしない。

gauntlet 自身に対する通し（dogfooding）:

```
gauntlet turn: pass (559ms)     差分なし
  ✓ typecheck (174ms)  ✓ tests (0ms)  ✓ crap (18ms)

gauntlet turn: fail (685ms)     未テストで CC 5 の関数を足した状態  exit 2
  ✗ crap  CRAP 30.0 (> 8)  複雑度 5 / 網羅率 0%  src/bad.ts:1 messy
```

### 5. `init` と setup skill — 完了

置くのは薄いファイルのみ: `.claude/settings.json` のフックエントリ / `gauntlet.config.json` / CI workflow / setup skill 1 枚。

キャッシュを温める工程は無い（ステップ 3 でキャッシュごと捨てた）。
`gauntlet doctor` は作らない — TypeScript の下限は gauntlet のチェック項目であって別コマンドではなく、今それを読む呼び出し元が無い。

**完了条件** — 素の TypeScript プロジェクトに `init` を打つと、その直後の最初のターンで `turn` が緑になる。
既にある `.claude/settings.json` を壊さない。

素の vitest プロジェクト（2 テスト）での通し:

```
$ gauntlet init
  gauntlet.config.json / .claude/settings.json
  .github/workflows/gauntlet.yml / .claude/skills/gauntlet/SKILL.md

$ gauntlet run --tier=turn          差分なし
gauntlet turn: pass (1390ms)  ✓ typecheck (603ms) ✓ tests (0ms) ✓ crap (27ms)

$ gauntlet run --tier=turn          未テストで CC 5 の関数を足した状態   exit 2
gauntlet turn: fail (670ms)
  ✗ crap  CRAP 30.0 (> 8)  複雑度 5 / 網羅率 0%  src/broken.ts:1 tangled
```

**新規ファイルにテストが無いと `tests` は 0 件で通る。** `--changed` が選ぶテストが無いため。
これは crap 側が網羅率 0 として捕まえるので穴にはならないが、2 つのチェックの合わせ技で成立している。

パッケージ名は **`@tepshq/gauntlet`**。開発中止した旧 gauntlet と同名で、registry 上のものは作り直す。
→ 0.0.14 の public npm 移行で **`@teps/gauntlet`** に改名（npmjs の org 名を teps に合わせた。
GitHub Packages の 0.0.13 以前は旧名のまま残る）。
→ 0.0.14 の「publish 済み」拒否を旧 gauntlet の焼け跡と誤診して **0.9.0 に跳んだ**が、
真相は「**0.0.14 自身の publish が成功していた**」（bin が壊れた版。npm 11.13 は bin の
`./` 接頭辞を invalid として黙って bin を削って publish する）+ 新規パッケージは作成直後
数分、読み取りが 404 を返す伝播ラグがある、の組合せだった。教訓: 新規 publish 直後の
404 は失敗の証拠にならない。`npm view <pkg> time` が正。番号は単調なのでそのまま続ける。

### 6. mutation testing（`pr` のみ）— 完了

Stryker + vitest runner。変更されたソースだけを `--mutate` に渡す。

**完了条件** — assert を取り除いた弱いテストを入れると `pr` が赤くなる。
その状態で `turn` は緑のまま（tier の役割分担が意図通り）。

**`--inPlace` が必須。** Stryker は既定でサンドボックスにコピーし、その過程で
`ts.parseConfigFileTextToJson` を呼ぶ。TypeScript 7 にその API は無く落ちる
（Volar や ts-jest が壊れているのと同じ根っこ）。`--inPlace` はコピーしないので前処理が走らない。
実ファイルを書き換えるが、mutation は CI でしか走らせないので作業ツリーは使い捨て。
Stryker 9.6.1 が最新で、修正版は無い。

`--since` は CLI に無い。`--mutate` が `file:startLine:startCol-endLine:endCol` の範囲指定を取れるので、
必要になればそちらで行単位に絞れる。今は変更ファイル丸ごと渡している。

`NoCoverage` は数えない。それは網羅率の話で CRAP が既に見ている。
mutation が独自に捕まえるのは「テストは通るが assert が弱い」ケースなので `Survived` だけを違反にする。

**変異対象は「変更されたファイル」ではなく「この差分で走ったテストが触れたソース」。**
変更ファイルだけを対象にすると、テストの assert を消しただけの差分では対応するソースが
変異対象から外れ、mutation ゲートを素通りできた（gameable）。完了条件の検証で実際に踏んだ。
coverage レポートに現れたファイルを渡すことで塞いだ。

gauntlet 自身に対する `pr` の推移:

| 状態 | Survived | 備考 |
| --- | --- | --- |
| 変更ファイルのみを対象 | 23 | うち 1 件は等価変異（`Stryker disable` で除外） |
| 上を全て潰した | 0 | 変異 347 件中 |
| 対象をテストが触れたソースに拡大 | **53** | 対象が広がって新しい穴が出た |

変異の種類（`StringLiteral` 等）だけで信号かノイズかは判断できない。
実際 `PreToolUse` の `matcher` を空文字にしてもテストが気づかなかった —
つまり「baseline ガードが起動しなくなっても緑」の状態だった。

**mutation にもファイル単位のラチェットを置いた**（DESIGN §3）。gauntlet 自身の種は:

```
config.ts 17 / complexity.ts 12 / coverage.ts 10 / ast.ts 8 / guard.ts 4 / その他 0〜1
```

**「触った関数の中の変異だけを絶対的に見る」は入れなかった。** ラチェットが既に
「増やさない」を保証しており、その上に 2 つ目の機構を足す根拠がまだ無い（CLAUDE.md の YAGNI）。
ラチェットだけでは足りない証拠が出たら足す。

gauntlet 自身に対する `pr`: **12.6 秒**で `lint`（未実装）以外すべて緑。

**baseline は記録が無ければ最初の実測値を種にする。** 0 から始めると既存リポジトリは
導入した瞬間に赤で埋まって誰も入れられない。gauntlet 自身では 12 が置かれた。

### 6.5 lint — 完了

eslint を `--format json` で叩き、ファイルごとのエラー数を mutation と同じラチェット
（`ratchetByFile`）に載せた。ルールは対象リポジトリが持つ。warning は数えない。

この過程で **TypeScript 7 の下限要求を撤回**した（DESIGN §5）。`typescript-eslint` の
peer 範囲が `<6.1.0` で、TS 7 では lint ゲートそのものが成立しないため。
gauntlet 自身も TypeScript 6.0.3 に落とし、`typescript` は runtime 依存から外した
（`oxc-parser` に切り替えて以降どこも import していない死んだ依存だった）。

ランタイム依存は **ajv / istanbul-lib-coverage / oxc-parser** の 3 つだけ。
→ 0.11.0 で **jscpd** が加わり 4 つ（重複ゲート。同梱してバージョン固定する理由は DESIGN §3）。

gauntlet 自身に対する `pr`: **14.6 秒で全 5 ゲート緑**（typecheck 0.5s / tests 0.7s /
crap 0s / lint 0.5s / mutation 12.8s）。テスト 203 件。

### CI の実測（tepshq/gauntlet#1、GitHub Actions）

| | ローカル | CI | 比 |
| --- | --- | --- | --- |
| mutation | 13.8 秒 | **86 秒** | **6.2 倍** |
| `pr` 全体 | 15.6 秒 | 92 秒 | — |

CI で確認できたこと: `merge-base` の `origin/main` フォールバックが効く、
`--inPlace` の Stryker が動く、全 5 ゲートが緑になる。

**mutation が予算の支配項。** hue はローカル 137 秒なので CI では 10 分超が見込まれる。
teps（2222 テスト）と duct はさらに大きい。実数が出てから、以下のどれかを検討する:
`--concurrency` を CI のコア数に合わせる / 対象を触った関数の行範囲まで絞る（保留していた案）/
mutation を別ジョブにして PR のブロックから外す。

### ~~private パッケージのアクセス（GitHub の手続き）~~ 0.0.14 で public npm に移行して解消

以下は GitHub Packages（〜0.0.13）時代の記録。認証事故のクラスごと消すため
public npm へ移行した（DESIGN §4）。消費側は `.npmrc` の scope 行と workflow の
認証（gauntlet のためだけのもの）を外す必要がある。

消費側リポジトリの `secrets.GITHUB_TOKEN` は、別リポジトリが所有する private パッケージを読めない。
hue の CI が `403 permission_denied: read_package` で落ちて判明した。

`Internal` 可視性なら解決するが、**tepshq は Team プランで internal が使えない**
（UI には出るが「organization administrators によって無効」と表示される）。
org レベルの secret に PAT を置く案は、寿命の長い個人トークンを org 全体の CI に置くことになるので採らない。

→ パッケージ設定の **Manage Actions access** で導入先リポジトリを個別に Read 許可する。
gauntlet の機能ではなく GitHub 側の手続きなので、`init` にも skill にも入れず README に書く。

### teps（進行中、`adopt-gauntlet` ブランチ）

測る範囲は対話で決めた。`tsconfig.include` は当てにならず、機械的には決まらない例:

| 除いたもの | 理由 |
| --- | --- |
| `tests/` | 133 ファイル中 132 がテスト。ここを測るとテストを測ることになる |
| `e2e/` | Playwright。vitest では走らないので coverage も mutation も意味を持たない |
| `scripts/` | デモと一度きりの移行スクリプト。出荷される製品コードではない |
| ルート直下の `.ts` | `next.config` `vitest.config` 等の設定 |

測る対象は `src` / `components` / `app` / `lib` の **170 ファイル**。

| | 実測 |
| --- | --- |
| `turn`（ソース 1 ファイル変更） | **10.4 秒**（typecheck 2.6s / tests 7.6s） |
| `pr` | **64 秒**（typecheck 2.6s / tests 9.6s / lint 2.3s / mutation 40.9s） |

`turn` の 10.4 秒は目標の 10 秒を超えている。テスト 7.6 秒が支配項で、
`src/dsl/schema.ts` のような中核ファイルは多くのテストから import されるため。

teps で見つけて直したもの:

- **`commands.typecheck` がシェルを通っていなかった。** teps は `tsc -p a.json --noEmit && tsc --noEmit`
  の 2 パスで、`&&` が tsc の引数になって `error TS5042` で落ちた。`sh -c` 経由にし、
  `node_modules/.bin` を PATH の先頭に置く。
- **eslint はマッチしない glob が 1 つでもあると即エラーで死ぬ。** 測る範囲の指定として
  `src/**/*.tsx` のように空になる組み合わせは普通にある。`--no-error-on-unmatched-pattern` を渡す。
- **eslint の失敗原因を握り潰していた。** 標準エラーにしか出ないので、報告に混ぜる。
- **`tsc` は出力に実行ビットを付けない。** ローカルパス install で `Permission denied` になる。
  ビルドで `chmod +x` する。

### 0.10.0: 走らせるテストは宣言制（2026-08-08）

integration という概念を gauntlet から完全に排除し、`tests.projects` に**宣言された
vitest project だけ**を走らせる正の選択にした（DESIGN §2）。固定名 `integration` の
除外規約は、hono（runtime project 5 つで固定名に集約不能）と duct main（名前だけの
規約が機構なしに放置）で破れたため。skill も `gauntlet` → `gauntlet-setup` に改名
（「gauntlet の全部」を名乗る大きさで、無関係な質問まで吸い込んでいた。init が旧
ディレクトリを片付ける）。

**破壊的変更**: 固定名 `integration` の自動除外は消えた。project を持つ導入済み
リポジトリ（duct）は、更新時に `tests.projects` の宣言を config に足す必要がある。

### hook 出力を自己完結にする（2026-08-08）

失敗の報告を「一行で、それだけ読めば直せる」（tier.ts の Violation の契約）に
揃えた。それまで typecheck と CRAP だけが自己完結で、残りは「落ちた事実」しか
言わず、読み手（エージェント）は道具を再実行しないと直せなかった:

- **tests** — ファイル名だけ → テスト名 + 失敗の本文（vitest の JSON に既にあった
  ものを `toOutcome` が捨てていた）。スタックと色コードは削り、ファイル自体が
  落ちた形（import エラー）はファイル側の `message` から拾う
- **mutation** — ファイルごとの件数だけ → 生き残りの行・mutator・変異後のコード
  （`survivedFrom` が捨てていた。無いと Stryker 再実行 = 分単位）
- **CRAP ラチェット** — 「N → M に増えました」だけ → 差分の外にある違反を名指し
  （テストを消して**触っていない**関数の網羅率が落ちた後退は、これが無いと
  手がかりがゼロ。記録は数だけなので、どれが増えた分かまでは言えない）
- **lint** — 件数だけ → 行・ルール・本文
- **詳細は 1 違反あたり 10 件で打ち切り、切った分は「…他 N 件」**（黙って切ると
  「これで全部」に読める）。`formatResult` は複数行の違反を全行段付けにした
- **guard** — 「編集できません」が、種置きの指示（コミットしてください）に従って
  `git add gauntlet.baseline.json` した読み手に矛盾して見えていた。止まる条件
  （ファイル名に触れること）と正規の経路（Read / `git add -A`）まで言う。
  BASELINE_NOT_COMMITTED と skill も同じ案内に揃えた
- **内部エラー** — 既知（ConfigError / GitError / RunnerError）はメッセージのみ、
  未知は gauntlet 自身のバグなのでスタックごと出す（`describeCrash`）

### 0.10.1: 「テストが触れた」の判定を実行実績に直す（2026-08-08）

README 監査のついでに、main.ts（どのテストも import しない）だけがソース差分の
`pr` を回したら Stryker が「No tests were executed」で落ちた。掘ると根が深かった:

- **vitest は `coverage.include` に合致する未ロードのファイルもゼロ行で coverage に載せる。**
  `coveredFiles` がキーの存在で「テストが触れた」を判定していたため、mutation の探索範囲が
  **include の全ファイルに膨張**していた（gauntlet 自身の `pr` が差分に関係なく毎回
  22 ファイルを変異させていた正体）。「1 文でも実行された」判定に直して 22 → 12 ファイル。
- テストが触れないファイルは変異させても全部 NoCoverage（数えない）なので対象から外し、
  外した数を scope に出す（`変異対象 12 ファイル（テストが触れない 1 ファイルは対象外 …）`）。
- init の警告文に残っていた旧 skill 名（`.claude/skills/gauntlet`）を直した。

### 0.13.0: pre-commit を全リポジトリ共通の起動点に昇格（2026-08-09）

duct 本導入（#567）で pre-commit 検問所が実証されたのを受け、「速いリポは Stop /
遅いリポは pre-commit」の二本立てをやめて**一本化**（ユーザー判断）。起動点が
リポジトリごとに違うと緑の意味の説明が 2 種類になる。

- `init` は `.githooks/pre-commit`（実行ビット付き）を書き、`Stop` フックを**書かない**。
  配線（`git config core.hooksPath .githooks`）は clone ごとに一度、手で行う —
  `init` の出力と skill が案内し、skill の完了条件に「わざと作った違反でコミットが
  拒否されること」を足した（配線忘れ = 走らないゲート、を潰す）
- 既にある `.claude/settings.json` の Stop フック（0.12 以前の導入や自前のもの）は
  消さない。0.12 以前からの移行は手で消す（skill に記載）
- gauntlet 自身も pre-commit に切り替え（dogfooding）
- `--no-verify` の穴は引き続き「破られた証拠が出てから」（DESIGN §2）

### 0.12.0: tier を検査範囲で改名（quick / full）+ サブコマンド直呼び（2026-08-08）

`turn` / `pr` は起動点の名前だったが、起動点が配備の詳細になって嘘になった —
duct の実測（実差分 25〜30 秒）で毎ターンの Stop は成立せず、**pre-commit を
検問所にする**運用を採用（「履歴に入った状態はすべて検査済み」の不変条件。
DESIGN §2 に起動点の選び方を記載）。手元で `pr` を回す種置き手順も既に
名前とずれていた。

- `npx gauntlet quick` / `npx gauntlet full` のサブコマンド直呼びに変更。
  `run --tier=` は廃止（旧形式は改名の案内を出して exit 2）
- 破壊的変更。採用がほぼゼロの今が最安のタイミング
- 未実装のまま記録: pre-commit 運用での `git commit --no-verify` 穴。guard で
  塞ぐかは duct 導入時に判断（gauntlet 以外の pre-commit があると一律には塞げない）

### 0.11.0: 重複ゲート + 測る対象を「リポジトリが所有するファイル」に（2026-08-08）

「持たない」と決めた重複ゲートを同日反転（ユーザーの「シンプルに収まるなら」）。

- **形**: `pr` のみ、repo 全体の**重複トークン数** 1 つを CRAP と同じ ratchet に載せる
  （絶対閾値なし・種を置いた回は通さない・改善で自動更新）。トークンなのは行だと
  整形だけで動くから。ファイル単位にしないのはクローンがファイル対に跨るから。
  **jscpd 5.0.14 を同梱・exact 固定**（対象側の何にも合わせる必要が無いので同梱できる。
  版がずれると同じコードで数字が変わる = flaky）。min-tokens 50 全社固定。
  対象は glob ではなく解決済みファイル一覧を渡す — CRAP と同一集合の保証。
  jscpd のファイル一覧渡しは決定的（同一入力 2 回で同数、実測）。
- **検証で別の大物を掘り当てた**: duct の重複が手動 29k に対し gauntlet は 169k。
  差は **gitignore された Prisma 生成クライアント 61 ファイル**（`postinstall` が
  `lib/generated/` に書き、`lib/**` に合致）。glob はディスクを見るので生成物を拾い、
  git ベースの差分検出は見ない — 2 つの世界がずれていた。
- **修正**: 測る対象 = glob ∩ `git ls-files --cached --others --exclude-standard`
  （追跡済み + 追跡外だが gitignore されていない = エージェントの書きたては残る）。
  `scopeReport`（init の「測る対象 N」）も同じ集合で数える（DESIGN §2）。
- **duct 再計測**: 重複 **29,484 トークン / 744 ファイル**（手動計測と完全一致）。
  CRAP の対象は 6669 → 6665 関数 — 生成コードは型が主で関数が少なく、CRAP 側の
  汚染は軽微だった。重複側は 140k トークンの水増しで、こちらが実害の本体。
- gauntlet 自身: 重複 0 トークン / 22 ファイル・約 100ms。全 6 ゲート緑（テスト 418）。

### 0.15.0: 導入の出発点を `npx @teps/gauntlet init` 1 コマンドに（2026-08-10）

h3 への手動導入で「先に何をどの PM で入れるか」を人間が考えていたのを潰した。
**CLI ウィザード案（TTY で対話）は検討して捨て**、skill に寄せた（ユーザー判断 —
「AI との対話？」→ いいえ、と答えたら「スキルでできる限り全部やるのがいい」）。
CLI に LLM を入れる案は flaky そのもの（走るたびに違う提案）なので最初から却下。

- **skill に「0. 依存を入れる」を追加** — PM 検出（`packageManager` フィールド →
  lockfile）、coverage-v8 の版揃え規則、install 実行まで skill が面倒を見る。
  README の導入も「init 1 コマンド → Claude Code で続きを」に一本化
- **実行時メッセージを PM-aware に** — 「入っていません。次で入れてください」の
  コマンドが npm 固定で、pnpm リポジトリで従うと lockfile が汚れた（h3 で気づいた）。
  `package-manager.ts`（検出 + install コマンド組み立て、20 テスト）を足し、
  Stryker / eslint の導入案内が正しい動詞（pnpm add -D 等）で出るようにした
- 対象の選定実測: 公開リポジトリ候補 6 本の package.json を検分 — zustand ◯、
  radash（jest）✗、es-toolkit（workspaces）✗、neverthrow / superjson（vitest 2.x /
  0.34）✗、**h3 ◯（TS 7.0.2 + vitest 4.1.10 + 単一パッケージ）を採用**。
  ~/dev/h3 にクローン済み、導入はユーザーが自走予定
- **化石をまた踏んだ**: gauntlet 自身の clone に 0.13 の `.githooks` +
  `core.hooksPath` 配線が残っていて（0.14 で PreToolUse に移行した際の
  撤去漏れ）、version bump 後の npx キャッシュ権限問題（既知）で commit が
  止まって発覚。skill の後始末どおり撤去。**「移行の後始末は移行した本人の
  リポジトリから」** — duct #567 の教訓（ドキュメントの化石）の配線版

### 0.18.0: lint ゲートを外す（2026-08-10）

きっかけは h3 で `full` が回らなかったこと（h3 は oxlint + oxfmt で eslint が無い）。
だが**外した理由は導入可否ではなく目的**（ユーザーの「gauntlet のそもそもの目的から
してどうか」）。

**判断の線**: gauntlet は「人間が読まない代わりに機械的な不変条件を担保する」。
だから中に置けるのは**担保している不変条件を言えるゲート**だけ。lint は
「ルールは対象リポジトリが持つ」と自分で宣言している以上、**何を守っているか言えない** —
`var` の禁止なのか設計制約なのかリポジトリ次第で、間違いと様式が混ざる。
一方 CRAP・mutation・重複・型チェックは、守っている性質を言える。

**型チェックは残す**（同じ問いを当てて検証した）:
- 間違いの定義が普遍的（意図と実装の食い違い）。リポジトリごとに変わらない
- 実測で示した: 型エラーのあるコードは**動いて黙って誤った値を返す**
  （`total(100, "2")` → `"1002"`）。tsc は赤、**テストは緑**、CRAP も捕まえない
- 界面が binary（文句を言ったか）なので、道具が増えても機構が増えない。
  実測 4 本中 3 本が既定 `tsc --noEmit` のまま、teps だけ 2 パスで
  `commands.typecheck` の**文字列 1 つ**で吸収できる。lint はラチェットのため
  per-file 件数が要り、ツールごとに JSON パーサが要った — そこが増殖の原因だった

副産物:
- **TS 7 下限を撤回した理由が消えた**（`typescript-eslint` の peer 制約）。
  ただし課す積極的な理由も無いので結論は変えない（DESIGN §5 に追記）
- gauntlet 自身の CI に `npm run lint` を追加（これまで `full` 頼りだった）。
  ついでに CI が 0.12.0 の改名から取り残されて `run --tier=pr` を叩いていたのを修正
- eslint が `.claude/worktrees/`（Claude Code のリポジトリ複製）を検査していたので除外
- baseline が許容していた `_argv` 未使用を解消 — **ラチェットが隠していた埃**が
  binary ゲートにしたら出てきた
- baseline の `lint` キーは `loadBaseline` が読まなくなったので **`full` が保存する
  時点で自動的に落ちた**（guard を回避せずに移行できる形）

### 0.18.0 以降、公開 tarball に死んだファイルが 6 つ混ざっていた（2026-08-12）

0.24.1 の publish 後、**公開された tarball と、ゲートを通したツリーが一致するか**を
確かめて見つけた（shasum が食い違ったので中身を展開して比べた）。共通ファイルは
全部バイト一致で、公開版にだけ 6 ファイル余分にあった:

```
dist/commit.{d.ts,js,js.map}
dist/typescript/lint.{d.ts,js,js.map}
```

どちらもソースが存在しない。`src/typescript/lint.ts` は **0.18.0 の「lint ゲートを外す」で
削除済み**なので、**それ以降のすべてのリリースに混ざっていた**ことになる。原因は
`tsc` が消えたソースの出力を消さないことで、長く使っている checkout の `dist/` に
溜まり続ける。今回気づけたのは、publish が新しく作った worktree（`npm ci` + build）から
行われて `dist/` が綺麗だったため — 本体の checkout から publish していたら差が出ず、
永久に気づかなかった。

**実害は無かった**（`exports` は `dist/index.js`、`bin` は `dist/cli.js` のみで、
この 6 ファイルはどこからも import されない。grep で確認）。それでも直すのは、
「配るのは検査したもの」が破れる経路であること、そして `npx gauntlet` が古い dist を
実行する既知の罠と根が同じであること。

`build` に `rm -rf dist` を置いた（DESIGN §4）。`prepublishOnly` 側ではなく `build` 側 —
`prepublishOnly` は `build` を呼ぶので publish も守られ、`npm run build` と「配られる
ビルド」が同じ意味になる。増分の犠牲は無い（実測 0.81 秒 → 0.66 秒。`build` の `tsc` は
そもそも増分ではない）。**効くことは実験で確認**した: 公開版に入っていた 6 ファイルを
`dist/` に仕込んで `npm run build` → 6 つとも消え、`npm pack --dry-run` も 72 ファイルに戻った。

### 複数 worktree 並行開発: baseline のマージ衝突を gauntlet 自身が解決する（2026-08-12）

「複数エージェント × 複数 worktree に耐えるか」の点検から。**プロセスの並行性は既に
設計済みだった** — 一時ファイルは全部 mkdtemp、mutation の二重起動ロックは root 単位
（worktree ごとに別）、git は cwd 起点。壊れるのは**複数ブランチが両方で記録を締めた後の
マージ衝突**で、しかも二重に悪い形だった:

- マーカー入りのファイルは JSON として読めず、「記録なし → 種置きで**負債の記録が全部消える**」に落ちる
- guard が出口を全部塞いでいる（`Edit` も `sed -i` も `git checkout --theirs` も止まる）
  ので、エージェントは自力で抜けられない

quick / full の冒頭で衝突を検出し、フィールドごとに厳しい側（crap / duplication は小さい方、
mutation はファイルの和集合 + `survived + timeout` が小さい record）で解決して書き戻す形に
した（DESIGN §3）。git の merge driver は clone ごとの `git config` 配線が要るので不採用
（`.githooks` を捨てたのと同じ理由）。実物の `git merge` が作る衝突は**部分衝突**（衝突しない
行は git が自動マージし、マーカーは食い違う行だけを挟む）なので、その形をテストで固定した。

**副産物: worktree 入れ子による場所依存の flaky を 1 件除去。** リポジトリ内
（`.claude/worktrees/`）の worktree で `full` を回すと、`vitestRunnerPlugin` の
`"package.json"` → `""` 変異が Node の親ディレクトリ遡りで**本体の node_modules に抜けて
生き残る**（本体の checkout や CI では killed — 同じコードで置き場所により答えが変わる）。
テストを「対象リポジトリの**中**で解決した」まで固定して場所非依存にした。

全ゲート緑（742 テスト、full 45.9 秒）。承知の上で残すもの: リポジトリ全体の 1 つの数
（crap / duplication）は並行ブランチをまたぐと緩む方向にずれる（DESIGN §7-10）。

### 自分のスコアを全量で測った（2026-08-12）

これまで自分の mutation は**差分に入ったファイルしか**測っていなかった。初めて全 src
23 ファイルを一度に変異させて、記録と実態を突き合わせた。

| | 実測 |
| --- | --- |
| mutation | 1793 / 1842 killed = **97.3%**（生き残り 49） |
| 測らなかった変異 | 174（静的 140 / 宣言して外した 34 / **理由なし 0**） |
| CRAP 違反 | 1 / 329 関数 |
| 重複 | 0 トークン |
| 網羅率 | Stmts 76.5% / Branch 86.9% / Func 78.7% |

**記録が実態より 3 件ゆるかった。** `guard.ts` 2 件・`baseline.ts` 1 件は既に死んでいた。
ratchet は**変異対象に入った回にしか締まらない**ので、差分に現れ続けなかったファイルの
記録は古いまま残る。0.22.0 で `list` から隠した `lint.ts` の死んだエントリも、記録本体は
残っていたので併せて消した（guard に止められるので人手で消す。ここは設計どおり）。

**config.ts の 16 件は、テストが外部ライブラリの文言に助けられていた。** 検査は
`toThrow(/defaultBranch/)` の形だったが、ajv の文自身が
`must have required property 'defaultBranch'` とキー名を含む。**`detailOf` を丸ごと
`""` にしても緑**で、gauntlet 側の整形は一行も検査されていなかった。gauntlet が足して
いる分 — 行頭の字下げ・`/` のルート表記・行末の `: <名前>`・行数 — をそれぞれ名指しで
見る形に差し替えて 16 → 1。`allErrors: true`（1 件目で切らない）と JSON パース失敗の
`catch`（素通しさせても別の理由で同じ `ConfigError` になる）も、これで初めて噛んだ。

> **教訓。** メッセージの検査は「含む」ではなく「**自分が足した分**」を見る。
> 外部ライブラリの文言が偶然テストを通すと、整形は無検査のまま緑になる。
> ajv の文言そのものには寄りかからない（版が変わると書き換わる）。

**#24 の「生き残りは単調ではない」を自分でも踏んだ。** `adapter.ts` は記録 0 だったが、
CRAP 違反の `analyze` を覆った途端に NoCoverage だった変異が測定に入り
（measured 39 → 57）、生き残りが 1 件出た。#24 の緩和（measured の増加分は許す）が
効く形ではあるが、**テストを足した回に同じファイルで新しい赤が出る**ことは変わらない。
1 件は報告の `adapter.version` を見ていなかっただけなので、そのまま殺した。

**disable の粒度は行。** 残った `strict: false`（現行スキーマでは strict でも同じ結果）を
#25 の `// Stryker disable next-line` で外そうとしたが、同じ行の `allErrors: true` は
**殺せる**変異だった。オプションを行に割ってから外している。最後に残る 1 件
（`allowedValues.join(" | ")` の区切り）は現行スキーマの enum が全て 1 値なので出力に
現れない — production を測定のために捻じるより、記録に残す方を選んだ。

`full` の所要は変異対象 6 ファイルで 20.0 秒（うち mutation 16.3 秒）。

### coverage.ts の境界を検査に入れた（2026-08-12）

生き残り 10 件が全部「文をどの関数に割り当てるか」の端（開始行・開始列・終了行・終了列と、
入れ子の内外判定）に集中していた。**ここを 1 つ間違えると全関数の網羅率が静かにずれ、
CRAP の数字が全部狂う**ので、gauntlet の中で最も検査が要る場所のひとつ。

端ごとに「1 つ内側」と「1 つ外側」で答えが変わるテストを 5 本足して **10 → 7**
（殺したのは 6 件、新しく測定に入った新顔が 3 件）。実測 78 変異。

残る 7 件は 2 種類に分かれ、**どちらもテストでは殺せない**:

**(1) 走査順が保証しているので、片方の枝に入らない — 5 件。**
`complexity.ts` の `walk` は関数を**先行順**で積む（`state.found.push(owner)` が
子への再帰より前）。したがって `innermost` の走査で後から来る候補は必ず内側にあり、
`startsLater` は常に true を返す。`a.startLine > b.startLine` を `>=` にしても、
`best === null || …` を `true` にしても、答えが変わらない。
**この不変条件は別のファイル（complexity.ts）にあり、coverage.ts 側には書かれていない。**
順序に依存しない今の書き方は、走査順が変わっても正しいままなので残す価値がある。

**(2) 端にちょうど乗る文が、この coverage provider では発生しない — 2 件
（`column >= startColumn` / `column <= endColumn`）。**

最初は「1 つ外まで届いている off-by-one では」と疑った。位置の定義はたしかに
半開区間で、`startColumn` は最初の文字（包含）、`endColumn` は最後の文字の**次**
（`function f() { const a = 1; }` は `end 1:29` でソースは 29 文字）。
だとすると `column <= endColumn` は閉じ括弧の直後から始まる文を内側と判定しうる。

**測ったら 0 件だった。** gauntlet 自身の 1055 文で、関数の開始位置・終了位置に
ちょうど乗る文は 1 つも無く、割り当てが変わる文も網羅率が動く関数も 0 / 339。
「gauntlet の書き方だからでは」を潰すため、わざと踏ませる形も作って直接確かめた:

```ts
export const curried = () => () => 1;                          // 外側の本体が内側の関数そのもの
export function tight() { return 1; }export const after = 2;   // 閉じ括弧の直後から次の文
```

| | 位置 |
| --- | --- |
| 関数 `curried` / 内側アロー | 1:23 / **1:29** |
| 文 | 1:13, **1:35** |
| 関数 `tight` の終了 | **3:37** |
| 文 | 3:26, **3:58** |

一致しない。**v8 の文の位置は宣言子や式を指している** — `const x = <関数>` の文は
関数の位置ではなく変数名の位置に付き、本体が関数そのものである簡潔アローには
文が作られない。だから端にちょうど乗る文が生まれない。

したがって `<=` を `<` に直しても、**振る舞いは変わらず、変異も殺せない**
（到達しない枝は直しても到達しない）。開始側の `>=` は半開区間の定義どおりで
そもそも正しい。**直さない。** 到達しないことが分かった以上、記録に残すのが正解。

**改善を測れる回と、記録できる回が噛み合っていない。** mutation の記録が締まる条件は
**clean なツリー**（#23）と**そのファイルが変異対象に入っていること**の両方だが、
main 直コミットではこの 2 つが両立しない — 測れるのはコミット前（差分に入るがツリーが
汚い）で、clean になるコミット後は差分が空になり変異対象が 0 に落ちる。実測: config.ts を
16 → 1 にしたコミットの直後に clean なツリーで `full` を回したところ、CRAP は 1 → 0 に
締まったが、**mutation の記録は 16 のまま**だった（`変異対象 0 ファイル`）。

上で見つけた古い記録（`guard.ts` 2 件・`baseline.ts` 1 件が実測 0）も、これで説明がつく。
締まらなかったのではなく、**締まる窓が一度も来ていなかった**。PR 運用なら feature ブランチの
clean なツリーで `full` を回す窓があるので噛み合う（gauntlet 自身は main 直コミット運用）。
記録が緩い方向にずれるだけなので緑の意味は壊れないが、**改善したのにゲートが緩いまま**残る。
→「一巡したら決めたいこと」に追加。

**→ 窓の存在を実測で確認（2026-08-12、下の「複数 worktree 並行開発」の作業中）。**
feature ブランチでコミットしてから clean なツリーで `full` を回すと、差分の起点が
`origin/main` なので変異対象は空にならず、**12 ファイル分の記録が一度に締まった** —
`config.ts` 16 → 1、`guard.ts` 2 → 0、加えて旧形式のまま残っていた 12 ファイルに
初めて母数（`measured`）と打ち切り（`timeout`）が入った。仮説どおり、
**機構の欠陥ではなく main 直コミット運用の副作用**だった。`full --record` のような
明示の記録回は要らない。

### ドキュメント総点検（2026-08-12）— 26 リリース分の沈殿を除去

イシュー対応の 2 日間、DESIGN / PLAN は同じターンに直す規律が守られていたが、
**その対応表に載らない場所**に古い記述が沈殿していた:

- **自分の `.claude/settings.json` が旧配線のまま**（guard + `if` 付き quick）。
  0.22.1 で「断続的に効かない」と捨てた `if` に、dogfooding 本体のコミット検問が
  依存し続けていた。`init` の叩き直しで 1 本の `hook` に入れ替え —
  「配線だけ入れ替わる」（0.16.0 の約束）の実地確認にもなった
- README: フック 2 つ → 1 本、`[npx gauntlet quick]` → `[npx gauntlet hook]`、
  「記録は数だけ」（mutation はファイルごとの 3 つ組に変わっていた）、
  `list` の説明（mutation の負債が載る）、実測表（398 → 691 テスト）
- DESIGN: 起動点の変遷表が `if` で止まっていた、§3 の「2 経路」の書き方、
  `list` の「mutation は含めない」（0.21 で読み出しを足した）、
  リスク 4（CRAP ≤ 8 は未検証 → 6 回止められ全て分割で解決、の一次情報に更新）、
  リスク 9（`if` 前提の記述を `hook` の文字列検出に書き直し）
- CLAUDE.md: 冒頭の tier 要約が「Stop フック / pre-commit」の時代のまま。
  「CRAP ≤ 8 は未検証」も dogfooding の実測に更新
- SKILL: probe の理由づけが `if`、「コミットは `git add -A` で行う」が
  0.21 の verb-based guard と矛盾（guard 自身が「add でも -A でも構いません」と言う）
- `run.ts` に迷子の docstring（「mutation の生き残りは含めない」— 直後の
  `mutationDebt` と矛盾）、`main.ts` の help も CRAP のみの記述

教訓: 変更のたびに直すルールは「どの文書のどこが影響を受けるか」を列挙できる範囲
でしか効かない。README と CLAUDE.md の冒頭要約、そして**自分自身のリポジトリの設定**は
どのリリースの変更対応表にも載らず、全部で 26 リリース分ずれた。リリースが落ち着いた
節目に全文書を現物（コード・出力・設定）と突き合わせる回を置く。

### 更新手順の初回実測（2026-08-12）— 使い捨てリポジトリで 0.21.1 → 0.23.4

skill の「更新する」は書き下ろしたまま一度も走っていなかったので通しで回した。

**duct を台にする案は不成立** — duct は gauntlet を導入していない（#567 は CLOSED）。
`node_modules/@teps/gauntlet` の残骸だけがあり `npx gauntlet --version` は 0.21.1 と
答えるが、`gauntlet.config.json` も `baseline` も無く、`.claude/settings.json` に配線が
入ったこともない。**版が答えることは導入の証拠にならない。** 代わりに duct が実際に
持っていた条件（既存の別フック・`/coverage` 記法の `.gitignore`・0.23 より前の
mutation 記録）を仕込んだ使い捨てリポジトリで測った。

| 手順 | 結果 |
| --- | --- |
| `skills add` で skill を入れ直す | 新しい `SKILL.md` が入る（`disable-model-invocation` 込み） |
| `npm i -D @teps/gauntlet@0.23.4` | 0.21.1 → 0.23.4 |
| フラグ無しの `init` | `gauntlet.config.json` は「変更なし」。**旧配線（`guard` + `quick` の 2 本）が `npx gauntlet hook` 1 本に置き換わり、無関係な PostToolUse フックは残った** |
| `quick` | pass（638ms） |

**旧形式の mutation 記録は自動では置き換わらない。** 0.23.4 で `full` を回しても、
変異対象にならなかったファイルの記録は `{"src/calc.ts": 2}` のまま残る。「記録の
`mutation` を `{}` にして回し直す」という後始末の指示は正しい。

**再現しなかったこと** — 「テストを足しただけで mutation が落ちる」は起きなかった
（1 ファイル 2 変異の規模では、テスト追加は変異を殺す側に働く）。後始末の各項目は
duct / h3 の実インシデントからの記録のままで、この実測が示したのは形式の差が
残ることだけ。

**ついでに出たバグ**（未修正）— `.gitignore` に `/coverage` があるのに `coverage/` を
重複追記する。末尾の `/` は突き合わせで無視されるが（0.18.2 で対応）、**先頭の `/` は
無視されない**。duct の `.gitignore` がこの記法だった。

### 0.24.1: 計測を中断したゲートの欄を種に書かない（2026-08-12、#28）

導入（step 6）の `full` で、**crap が「vitest が網羅率を測っていないファイルがある」で
計測を中断したのに、記録に `"crap": 0` が書かれた**という報告。0 は最も厳しい値なので、
範囲の不整合を解消した次の回が「0 → 772 に増えました」で落ち、guard が記録の書き換えを
止めるため**エージェント側に出口が無い**（報告者は人手で消して復旧）。#27（mutation の
0/0/0）と同じ形が crap 側に残っていた。

真因は crap ゲートではなく**他のゲートの埋め草**。`EMPTY_BASELINE = { crap: 0,
mutation: {} }` を duplication と mutation が記録を書くときに spread していたので、
crap が 1 度も書いていない 0 がディスクに残る。構造的には `Baseline.crap` が必須の
`number` で「まだ測っていない」を表現できなかったこと — duplication は optional、
mutation はファイル単位の省略で、同じ判断が既に入っていた。**3 つのゲートのうち
1 つだけ「無い」を持てなかった。**

- **`crap` を optional に。** 完走したゲートだけが自分の欄を書く
- **`loadBaseline` は欄ごとに読む。** 従来は `crap` が数でなければ記録ごと null で、
  それだと crap 欄の無い種が「記録なし」に見え、duplication と mutation が毎回種を
  置き直す。null は「JSON オブジェクトとして読めない」だけに狭めた
- **`ratchet(baseline, actual)` を畳んだ。** 残すと「記録が無ければ種」の判定が
  `gateRepository`（null）と `ratchet`（欄なし）の 2 か所になる。`ratchetNumber` に一本化
- **書けなかった回に「作りました」と言わない。** notes（「clean でないため保存して
  いません」）を種置きの案内より優先する。crap 欄だけを後から置く回は dirty なツリーで
  普通に起きる（不整合を直した編集が未コミット）ので、この形が踏みやすくなった

**使い捨てリポジトリで修正前後を通しで測った**（vitest 4.1.10、2 ファイル 2 関数、
違反 1 件、mutation は変異対象 0）:

| | 0.23.4 | 修正後 |
| --- | --- | --- |
| crap が中断した回の種 | `{"crap": 0, "mutation": {}, "duplication": 0}` | `{"mutation": {}, "duplication": 0}` |
| 不整合を解消した次の回 | `違反が 0 → 1 に増えました`（恒久的な赤） | `許容値の記録を作りました` → `crap: 1` |
| その記録をコミットした回 | 赤のまま | `pass` |

**報告の引き金（vitest の既定 `coverage.exclude`）は 4 系では再現しない** —
`coverageConfigDefaults.exclude` が `[]` になっている（vitest 4.1.10 で実測）。だから
実測は明示の `coverage.exclude` で同じ断面（測る対象なのに vitest が測らないファイル）を
作った。報告環境は 3.2.6 で、既定の `**/*{.,-}{test,spec}?(-d).?(c|m)[jt]s?(x)` が
`connection-test.ts` に当たっていた。SKILL の step 2 には「設定ファイルには現れないので
**ファイル名の側から**探す」を足した。

**既に `crap: 0` が焼かれた記録は救えない** — 実測の 0（違反ゼロ）と区別が付かない。
人手のリセットが要る（報告者はそれで復旧済み）。

**0.24.0（衝突の自動解決）を別セッションが同時に作っていて、合流で穴が 1 つ出た。**
`mergeBaselines` は `crap: Math.min(ours.crap, theirs.crap)` で、crap が optional に
なると**片側に欄が無ければ `NaN` を記録に書く**（以降どんな実測とも比べられない）。
duplication には既に「無い方は有る方が厳しい」の関数があったので、crap と共有する
`tighterCount` に一般化した。

**この穴はどちらのブランチでも型エラーにならない。** `Math.min` に
`number | undefined` を渡すのは TS2345 になる（実測で確認）が、それが成立するのは
**合流後の木だけ** — 0.24.0 側には `crap?: number` が無く、#28 側には `mergeBaselines` が
無いので、マージするまでどの `tsc` も見ていない。型は最後には捕まえるが、**捕まえる
瞬間が合流後にしか来ない**。並行して同じファイルを触るときは、型が交差する場所
（optional にした欄を読む全箇所）を合流時に自分で洗う必要がある。

**残る粗さ**: dirty なツリーで crap の種置きが起きる回は、ゲートの行が「許容値の記録を
作りました」と言い、footer が「保存していません」と言う。値の判定は正しく footer が最後に
真実を言うが、1 つの出力の中で食い違っている。ゲートごとの印に persist を渡す形
（全ゲートの引数が増える）を避けた結果。

### #27 クローズ（2026-08-12）— 実害の大きさが実測で確定

0.23.3 で報告者の `full` が本物として完走（39 秒、レポートの mtime・ファイル数が
実行と一致）。**0/0/0 に潰されていた styleTypes.ts の実測は生き残り 22 件** —
あの記録のままなら 22 件が素通りした上で、以後どれだけ増えても落ちなかった。
旧形式のまま残っていた 3 ファイルにも初めて本物の母数と打ち切りが入った。

真の致命傷（920 件の前処理連鎖のどこか）は、前処理ごと消えたため未特定のまま。
`lastReasons` の改善（最後の ERROR から見せる）は残るので、別の失敗では本体が見える。

### 0.23.3: 前処理が gitignore 済みの HTML を食べていた（2026-08-12、#27 の第 3 弾）

0.23.2 で露出した本物のエラーは `setTimeout` の spy ではなく、**Stryker の型チェック
無効化前処理が、gitignore 済みのクロールキャッシュ（HTML 920 ファイル）を Babel で
解析して壊れる**ものだった。報告者が mtime で分かれ目を特定（43 店舗クロールで
Babel を落とす HTML が増えた 10:31 が境界。gauntlet の設定は 0.22.1 から不変で、
**版を戻しても回避できない** — 落とす入力が無かったから通っていただけ）。

- **`disableTypeChecks: false`。** この前処理は `@ts-nocheck` を差し込むためのもので、
  gauntlet は mutation の内側で型を見ないので何の役にも立っていなかった。
  「inPlace なら前処理が走らない」という DESIGN の記述は 9.6.1 では誤りで、訂正した
- **`lastReasons` は最後の ERROR 行から先を見せる。** 920 件の前処理警告が末尾 15 行を
  埋め、致命傷が窓の外に押し出されていた（私の再現失敗の原因もこれ — 前処理は
  per-file で catch して WARN するだけなので、1 ファイルでは死なない）
- 手元に毒 HTML を置いた再現で、修正後は落ちずに完走することを確認した

### 0.23.2: 古いレポートを黙って読んでいた（2026-08-12、#27 の追試）

報告者が mutation.json の mtime を確認し、**0.23.x では full を回してもレポートが
書かれていない**（0.22.1 の残骸がずっと残っていて、13 > 10 の食い違いはそれを読んだ
取り違え）ことを特定した。同じ 10 ファイルで所要が 50998ms → 5136ms という 10 倍差も
添えられた — 彼らの環境では Stryker が早期に失敗していて、**gauntlet は失敗に気づかず
古いレポートから記録まで更新していた**。

原因はまた**二つの決定の相互作用**: 「レポートが無ければ失敗」（0.20 からの検出）は
前回の残骸が無いことが前提で、0.21.0 の「実行後に消さない」（#20 対応）がその前提を
壊していた。#27 本体（0/0/0）と同じ形の教訓が、同じ issue の中で 2 回出た。

**走らせる直前に前回のレポートを消す**。前回の分は次に回すまで残るので、#20 の
「後から見る材料」は保たれる。e2e で両方向を確認した — 残骸がある状態で成功すると
新レポートに置き換わり、失敗すると残骸を読まずに Stryker の出力つきで落ちる。

彼らの環境で Stryker が 5 秒で失敗する根因はこの修正で**見えるようになる**
（次の実行が lastReasons つきで落ちる）。そこから先はその出力待ち。

### 0.23.1: 測られなかったファイルに 0/0/0 を書いていた（2026-08-12、#27）

0.23.0 への移行で、**測られていない 4 ファイルの記録が `{survived:0, measured:0,
timeout:0}` に上書きされた**（17 件・19 件・2 件の負債が記録上 0 に）。変異対象の
候補に入っても Stryker の報告に現れない経路は普通にある — 型定義だけのファイル、
変異の作れないファイル。`counts[file] ?? 0/0/0` がそれを「実測」として扱っていた。

**実害の向きが最悪だった**: measured:0 の記録は slack = actual.measured になり、
survived + timeout は measured の部分集合なので**必ず通る** — そのファイルのゲートが
恒久的に無効になり、通るたびに 0/0/0 が書き直されるので自己回復しない。
「厳しくなる方向の誤り」ではなく「静かに緩む方向の誤り」。

- **測定結果が無いファイルは記録に触らない**（突き合わせも更新もしない）。
  「全部殺した」（報告にあり、生き残り 0）と「測られていない」（報告に無い）は別物
- **二重の歯止め**: measured が 0 の記録には余裕を作らない。既に汚染された記録は
  静かに通り続けるのではなく、大声で落ちて人の目に入る
- **0/0/0 を固定するテストを自分が書いていた**（「生き残りが無くなれば 0 にする」）。
  初期からの動きで、survived だけの時代は 0 = 最厳格なので無害だった。measured の
  余裕（0.22.0）が入った時点で毒に変わっていた — 仕様の相互作用は、単体では
  無害な 2 つの決定の間で生まれる

### 0.23.0: 打ち切りは環境の関数だった（2026-08-12、#26）

同じコミット・同じ版で mutation が**手元では通り CI では落ち続けた**（常に +3。
何度締め直しても CI が超える）。報告者が両環境の mutation.json を突き合わせ、
**Killed が 344 で完全一致・Timeout の差（4→1）が Survived の差（66→69）に対応**、
原因は 1 行の `sleep(backoffMs(attempt))` と特定した — 打ち切りの閾値
（`timeoutMS + timeoutFactor × 通常実行時間`）が実行速度に比例するため、
「遅くなるだけの変異」が速い機械では Timeout（殺した扱い）、遅い CI では Survived になる。

**提案 1（閾値を絶対値に）は採らなかった。** sleep は絶対時間・計算は環境速度に
比例するので、どんな閾値でも環境不変にならない。環境で動かないのは Killed —
**その補集合 survived + timeout を突き合わせる**（記録に timeout を足した。0.22 以前の
記録は survived だけで比べ、通った回に新形式へ移る）。報告の実測がそのまま検算になる:
66+4 = 69+1 = 70。副作用も正しい向きで、「時計で殺した」は返済に数えられなくなり、
数字を減らす道は assert で殺すことだけになる（報告者の直し —
経過時間ではなく `setTimeout` の呼び出しを検証 — がまさにそれ）。

提案 2・3（記録と表示）はそのまま採用。落ちたときの文言に
`（打ち切り 4 → 1、測った変異 414 → 414）` が入り、今回の切り分け（CI から
artifact で mutation.json を持ち帰る）が gauntlet の出力だけで終わるようになる。

この回も自分のゲートに止められた — timeout の突き合わせを足した `ratchetByFile` が
CC 11・網羅率 100%（jotai で観察した「テストでは通らない」形そのもの）。
`fileRegression` に割った。

### 0.22.2: クラッシュ窓を消す / 除外の内訳を出す（2026-08-12、#23 #25 の追試）

- **#23 「書いてから書き戻す」にはクラッシュ窓があった。** 各ゲートがディスクに書き、
  実行の最後に書き戻す形だと、その間（大半は mutation の数十秒〜分単位）に死ぬと
  作業途中の値が残る — 報告者は実際に**使用量上限でプロセスを kill されている**。
  記録の読み書き口を `BaselineStore` にし、書けない実行ではメモリに差し替えた。
  ゲートは普段どおり書き、ディスクには触れない。知らせは動いた中身つきになった
- **#23-2 clean は「正しい基準」ではなく「意図的な状態」。** フィーチャーブランチの
  中間コミットの値も基準になる。コミットは意図的でレビューが利くので受け入れ、
  merge-base の測り直し（mutation もう 1 周）は持たない — DESIGN に整理を書いた
- **#25 除外の内訳。** static の除外と disable の除外がどちらも `Ignored` で、
  意図的な除外の総数が誰にも見えなかった（「見えていれば人が気づける」を軸に
  disable 方式を選んだのに）。`statusReason` で二分して
  `（静的な変異 N 件・宣言して外した変異 M 件は測っていません）` と出す。
  **理由の無い disable も件数で言う**（落としはしない）— 理由必須の原則を仕組みで支える
- **#24 は報告者の追試で閉じ。** 「measured を増やして余裕を買う」抜け道を自分で
  測り直し、no-assert テストでは増分がほぼ相殺する（残るのは例外・timeout で死ぬ
  変異の分だけ）と確認された。実測せずに断定しない、が双方向に働いた形

### 0.22.1: `if` を捨てて、フックを 1 本に（2026-08-12、#22 の追試を受けて）

#22 の報告者が追試で 2 つ正した。(1) **同一マシン・同一設定**なので「版が古い」は成立
しない。(2) 実態は「全 Bash で走る」ではなく**「断続的に走る」** — tsbuildinfo の mtime
で quick の実行有無を観測する方法まで示された（いまは `if` が効き、昨日はループや
リダイレクトを含む複合コマンドで 5 回以上発火した）。

疑いはパターンの綴り（`Bash(git commit *)` — 正しくは `Bash(git commit:*)` のコロン形
かもしれない）。不正なパターンの扱いが経路によってぶれるなら断続性と整合する。
**私の「docs で確認済み」も撤回** — プロンプトに自分の綴りを入れて聞いた誘導質問で、
要約がそれを鸚鵡返しした可能性が高い。

決め手は綴りではなく**失敗の非対称**（報告者の指摘）: `if` が緩く無視されると
「うるさい」側だが、厳密に解釈される経路に乗ると **`git commit *` は何にもマッチせず
検問が黙って消える**。precommit が自己判定する以上、`if` は危険だけ残して益が無い。
**`if` を使うのをやめた。**

`if` 無しで guard と precommit を別々に置くと全 Bash が node を 2 回起動するので、
**1 本の `hook` に統合**（block → quick → pass。block が先 — 記録の書き換えを含む
コミットは検問の前に止める）。init は 3 世代の旧配線（guard 単独 / quick 直呼び /
precommit + if）をすべて撤去して置き換える。

作業中に guard の順序バグも踏んだ — **区切ってから剥がす**と、引用の中の `&&` で
引用が千切れて散文が裸になる（自分のテストデータを書くコマンドで実測）。
剥がしてから区切る形に直した。

### 0.22.0: CRAP 撲滅作業からの報告 4 件（2026-08-11、#22〜#25 + #20 追記）

初めて「赤を減らす作業」を通しでやった人からの報告。ゲートが正しく判定する場面では
なく、**ゲートに従って作業する人の動線**が壊れていた。

- **#22 `if` を知らない版の Claude Code で、全 Bash に quick が走る。** `if` は公式機能
  （docs 確認済み。この環境では効いている）だが、知らない版は未知フィールドを黙って
  無視する。作業ツリーが赤い間は復旧の git コマンドまで止まり、報告者は tsconfig の
  exclude で**ゲートを外して**復旧した — ゲートが自分の直し方として最悪の抜け道を
  教えた形。コミット検出を gauntlet 自身（`precommit`、stdin の JSON を読む）に移し、
  `if` は node 起動を省く最適化に格下げした。init は旧 quick 直呼びフックを撤去する
- **#22-4/#20 guard がファイル名の散文まで止める。** issue の本文にファイル名が
  あるだけで `gh issue create` が止まった（**この修正の作業中に私自身も heredoc で
  同じ誤爆を踏んだ**）。判定を動詞側（書く形 × 行き先）に反転し、引用の中は見ない。
  引用されたパスで書く形はすり抜けるが、guard は柵であって金庫ではない —
  名指しを避けた書き換えは元から止めようがない
- **#23 作業途中の値で記録が締まる。** 分割の途中の 77 が刻まれ、直後の編集が 80 で
  落ちた（77 は既定ブランチのどこにも無い、本人の中間状態）。stash 中の実行が記録を
  汚して pop が衝突する事故も同根。**clean なツリーの実測だけを記録する** —
  チェックは普通に書き、実行の最後に clean でなければ書き戻す（`settleBaseline`）。
  種置きは例外（init 直後は必ず未コミット）
- **#24 生き残りの数は単調ではない。** テストを足すと ignoreStatic で外れていた変異が
  測定に入り、**触っていないファイル**の生き残りが 39 → 41 に増えて落ちた。許容値と
  実測が別のテスト集合の下の数字で、比べられていなかった。記録に「測った変異の数」を
  足し、**測定集合が広がった分だけ増加を許す**。assert 削除は measured が変わらないので
  今までどおり落ちる（gameable の穴は開けない）。旧記録は厳格なまま、通った回に移行。
  新規ファイルへの初回記録は名指しで出す（ファイル移動で借金が黙って消える穴への可視化）
- **#25 等価変異は新しい仕組みを作らない。** `// Stryker disable next-line X: 理由` が
  既に使える（gauntlet 自身が使っている）。コードと動くので行ずれで失効せず、理由が
  レビューに乗る。README に書いた。設定ファイルの無視リストは作らない —
  エージェントが書ける場所に「外す」余地を増やさない

### 0.21.1: dry run の打ち切りが CI で必ず噛んでいた（2026-08-11、#21）

duct の CI で mutation が落ちた。**手元では通り CI だけで落ちる** — 他の 4 ゲートには
無い形。`strykerConfig` が `dryRunTimeoutMinutes` を設定しておらず、Stryker の既定
**5 分**が効いていた。dry run は全スイートを perTest coverage 付きで走らせるので、
duct（手元 51 秒 / CI 9 分 35 秒）では原理的に収まらない（5 分 02 秒で打ち切り）。

**gauntlet は 2 つ目の締切を持たない**という形にした。止める役は CI job の
`timeout-minutes` で、それは使う側が選んだ数。誰も選んでいない 5 分がその外側から
先に噛むのが問題だったので、60 分を置いて**先に噛まない**ようにする（異常なハングを
最後に捕まえる網であって、予算ではない）。

skill の timeout 確認にも「mutation が走る回は全テストの所要 × 数回」を足した。

### 0.21.0: 新規リポジトリの導入で出た 4 点（2026-08-11、#20）

新規リポジトリ（TypeScript 20 ファイル・テスト 0 件・CI なし・npm）への導入から。

- **guard が読み取りと `git add` まで止めていた。** 止めたいのは書き換えなのに、
  ファイル名に触れる Bash を一律で止めていた。`git diff` は読むだけ（案内していた
  Read ツールでは差分が見られない）、`git add <path>` は書き換えない。その結果
  `git add -A` が唯一の道になり、**並行作業の無関係なファイルを巻き込んだ**
  （報告者が実測。私も同日に同じ形の事故を踏んでいる）。区間ごとに見て、
  読み取り専用の git と `git add` は通す。`restore` / `checkout` は止めたまま —
  **締まった記録を緩い値に戻せる**ので、それは基準を緩める操作
- **`full` が記録を締めたことを言わなかった。** 気づいたのは `git status` に出ていたから、
  という状態。コミットし忘れると CI は緩い値を使い続ける。**種置きには案内があるのに
  更新には無い**という非対称だった。実行の前後で記録を突き合わせて出す
  （各ゲートに手を入れずに済む）
- **`list` に mutation が出なかった。** 「Stryker のフル実行が要るから含めない」と
  0.18.5 で決めたが、**件数は記録の中にあるので読み出すだけ**だった。「どの変異か」と
  「何件か」を一緒くたにしていた。件数を多い順に出し、詳細の在り処だけ言う。
  併せて **Stryker のレポートを消すのをやめた** — 読んだ直後に消していたので、
  生き残りを見るには分単位の再実行しかなかった
- **skill の CI 例が PR 運用前提だった。** 直コミット運用のリポジトリに
  `on: pull_request` を置くと一度も走らない。**提案された `on: push` は採らなかった** —
  それは通知であってゲートではない（既にコミット済みのものを事後に検査するだけ）。
  「設定はあるのに実質存在しない」という同じ失敗になる。PR 運用へ移すのを勧める形にした

**この回も自分のゲートに止められた。** `ratchetNote` が CC 9・網羅率 100% で CRAP 9。
jotai で見ていた「よくテストされているが複雑すぎる」とまったく同じ形で、
助言どおり分割した（`ratchetChanges` に切り出し）。

**自分の記録に消えたファイルが残っていた** — 0.18.0 で消した `src/typescript/lint.ts`。
`list` に mutation を出すようにして初めて見えた。ratchet は締める方向にしか動かず、
対象から外れたファイルの記録は二度と触られないので残る。並べても探しに行けないだけなので、
今あるファイルだけ出すようにした。

### 赤を減らす作業を jotai で覗いた（2026-08-11）

「スコアを改善していくときはどうするのがいいのか」を確かめるために、jotai の
CRAP 違反 6 件を 1 件ずつ中身まで見た。**結果は「安い勝ち筋が 1 件も無い」。**

| 件数 | 状態 | 直し方 |
| ---: | --- | --- |
| 4 | 網羅率 98〜100% で複雑度 11〜17（`internals.ts` の中核 3 つ + `splitAtom`） | **分割しかない**。よくテストされた性能上の要でもある |
| 1 | 網羅率 0% / CC 4（`useAtomValue.ts` の `React.use` フォールバック） | **この環境では到達不能**。React 19 では実行されない版差の shim |
| 1 | 網羅率 88% / CC 8（`mountDependencies`） | 未実行は 3 行。依存の epoch がずれる並行経路で、**書くには内部意味論の理解が要る** |

**「テストを 1 本足せば消える」が 0 件。** h3 でも違反 35 件のうち 25 件が網羅率
90〜100% だった。よく手入れされたライブラリでは、導入後に残る借金は**構造的なもの**に
偏る（借金の性格はリポジトリの成熟度で決まる — アプリは「未テストの複雑さ」、
ライブラリは「テスト済みだが密な中核」）。

**版差の shim という 4 つ目のバケツが見つかった。** 消せず・テストできず・直せない。
ratchet はこういうものを恒久的に抱えるためにある、という位置づけがはっきりした。
違反を 0 に追い込むのは目標ではない。

**skill にはしない判断の根拠にもなった。** 仕分け（テストで通るか / 割るか / 放置か）は
既に違反 1 行ごとの助言で出ており、その先（jotai の store を割るべきか）は
gauntlet が持たないと決めた種類の判断。手順化できる余地が薄い。

### jotai — jsdom でも Stryker は回った（2026-08-11）

3 つ目のパイロット。h3（node のサーバ）・duct（Next.js の大規模アプリ）に対して、
**React + jsdom** という未検証の形を当てるために選んだ。

| | h3 | duct | jotai |
| --- | --- | --- | --- |
| 実行環境 | node | node | **jsdom** |
| テストの性格 | HTTP | 統合寄り | **React Testing Library** |
| vitest 設定 | 素 | projects | **`globals: true` + setupFiles + vite プラグイン** |
| 測る対象 | 50 ファイル / 411 関数 | 794 ファイル / 6808 関数 | 42 ファイル |
| CRAP 違反 | 35 | 801 | **6** |
| 重複トークン | 109 | 30196 | **206** |

**`doctor` が通った** — `--inPlace` の計装コードを jsdom 環境の vitest が読み、Stryker の
初回実行が成立する。ラッパーが `globals` / `setupFiles` / プラグイン付きの設定を
正しく引き継げることも確かめられた（ここが一番壊れやすいと見ていた）。

**CRAP 違反 6 件は 3 リポジトリで最少。** 42 ファイルという規模を割り引いても、
関数が小さく保たれているライブラリでは閾値 8 がほとんど当たらない、という方向の
一次情報になる（CLAUDE.md の「CRAP ≤ 8 が実際の TypeScript でどれだけ厳しいか」）。

### 0.20.4: テストが 0 件のときの doctor（2026-08-11、#19）

vitest はあるがテストが 1 件も無いリポジトリ（新規導入直後）で `doctor` が Stryker の
生スタックで落ちていた。**Stryker はこれを `ConfigError`（"Please check your
configuration"）として投げ**、vitest runner も「`vitest.related` を切れ」と警告するので、
**設定を疑う方向へ誘導される** — 実際の原因は「テストが 0 件」で、対処は正反対。

さらに **SKILL.md の完了条件が原理的に満たせなかった**（step 6 が `doctor` の成功を
要求するのに、テスト 0 件では必ず落ちる）。手順どおりに進めた導入が閉じない形。

Stryker の文言（`No tests were executed`）を読んで、次にやることだけを言う。
**exit は 2 のまま** — 確かめられていないものを緑にしない。skill は step 1（要件）に
「テストが 0 件なら先に 1 件書いてもらう」を足した。

判定を 1 つ足した時点で `dryRunMutation` が CC 3・網羅率 0 の **CRAP 12** になり、
**自分のゲートに止められた**。判断を `dryRunFailure`（純粋関数）に出して解いた —
「プロセスを起動する殻には判断を残さない」という元からの方針どおりの形に落ちたので、
**閾値が設計を正しい方向に押した**例。

**Stryker はテスト 0 件のとき非ゼロで終わらないことがある**ので、終了コードだけを
見る作りだと生スタックのまま素通りする。出力を先に見る形にした。

### 0.20.3: 関数 hit だけでは足りなかった（2026-08-11、#18）

0.20.0 の #12 対応（文 hit → 関数 hit）が、duct で**変異対象を 18 → 700 に増やした**。
`--coverage.include` を渡すようになった 0.19.0 以降、テストがロードしていないファイルも
coverage に載る。その合成エントリの一部が「文は全部 0 なのに関数 hit 1」になっていた。

**両方を要求する形に直した。** 実際に走ったなら関数の中の文も走るので、
`関数 hit > 0 かつ文 hit = 0` は現実の実行では起こりえない。この AND が落とすのは
計測の作り物だけで、本物は落とさない。**duct で end-to-end を確認済み**
（coverage のエントリ 853 / 変異対象 1 ファイル。18 → 700 → **1**）。

**合成エントリに関数 hit が付く現象は再現できなかった。** vitest 3.2.6（報告環境）と
4.1.10（gauntlet）の両方で、未ロードのファイルは `f` も 0（定数だけ・default export・
アロー関数など形を変えても同じ）。報告者が `@vitest/coverage-v8` の
`(empty-report)` プレースホルダまで辿ったが、`count: 0` を渡しているのに 1 になる
一段は未解明。**AND の正しさが原因の解明に依存しない**ので、待たずに直せた。

教訓: **#12 の最小再現には未ロードのファイルが登場しなかった。** 4 つとも import 経路に
載っていたので合成エントリが現れず、再現しても気づけない形だった。**実際の呼び出しと
同じ引数で測る**（この場合は `--coverage.include` を渡す）ところまでやらないと、
再現の意味が薄い。

### 0.20.2: h3 の新規導入で出た 3 点（2026-08-11、#17）

0.19.3 の h3 への**新規導入が一発で通った**回の報告。導入を止めるものは無く、
どれも「読み手が誤解する」形をしていた。

- **`--version` が無かった。** skill の step 1 の完了条件が「入った版が npm の latest と
  一致していること」なのに、gauntlet 自身に訊く手段が無い。pnpm の `minimumReleaseAge`
  があるので「指定した版と実際に入った版が違う」は現実に起きるのに、確かめる正攻法が
  無かった。`--version` / `-v` を足した（版だけ出して exit 0）
- **`.gitignore` の追記が `coverage` と `coverage/` を別物として扱っていた。** 前者は
  ディレクトリにも当たるので**既存の方が広い**。突き合わせで末尾の `/` を無視する
- **種置きの説明がゲートの数だけ並んでいた。** 記録は 1 ファイルなので事象は 1 つなのに、
  crap と duplication に同じ 2 行が出て「別々に何かが起きた」と読める。ゲートの行は
  短い印にして、説明は最後に 1 回だけ

### 0.20.1: 冗長になったテストの除外を落とす（2026-08-11）

0.20.0 でテストが自動で外れるようになったので、`init` の既定 `exclude` と README の例が
「テストは自分で外すもの」と読ませる形で残っていた。既定を空にし、除外が無ければ
キー自体を書かない。skill の「古いバージョンから上げる場合」に、**0.20.0 より前に
mutation を回した記録は触っていないファイルの生き残りを含みうる**（duct で 17 ファイル
587 件）ことと、記録の `mutation` を空にして置き直す手順を足した。

### 0.20.0: duct の報告 5 件（2026-08-11、#12〜#16）

duct（製品コード 794 ファイル・テスト 7354 件・Next.js + Prisma）への導入から。
h3 では出なかったものばかりで、**規模と形が変わると別の穴が出る**という実例になった。

- **#13 測る対象にテストが入っていた。** duct は 1475 ファイルのうち 681 がテスト。
  `vi.fn().mockImplementation()` の `switch` が CRAP 110 で、テストを 1 行直すと
  コミットが止まる。重複も 133437 → 30196 トークンで、差の 10 万は定型文だった。
  `isTestFile` は既にあり mutation にだけ使われていた（「守るテストが無いので必ず
  生き残る」）。**同じ理由が CRAP と重複にも当てはまる**ので `listSourceFiles` で落とす。
  **step 5 の検算がこの状態を通していた**のが一番痛い — step 2 で数えるのはテスト込みの
  数なので、1475 でぴったり一致して「検算済み」として確定した。検算もテスト抜きに直した
- **#12 バレル import で変異対象が膨らむ。** `coveredFiles` が「1 文でも実行された」で
  判定していたため。最小再現で確かめた（`export const B = 2` だけのファイルが
  文 hit 1 / 関数 hit 0）。関数 hit で判定する形に直した（→ 0.20.3 で AND に）
- **#14 duplication の「対象 N ファイル」が渡した数と違う。** jscpd の `sources` は
  min-tokens 以上のファイル数。gauntlet 自身でも再現した（渡した 23 に対し 22）
- **#15 打ち切られた回に何も出ない。** duct の CI で 2 回。17 分走って出力ゼロで、
  「壊れているのか・設定が違うのか・単に遅いのか」が区別できなかった。`full` だけ
  段の開始を 1 行出す（`quick` は数秒で終わり、出力がフック経由でエージェントの
  文脈に入るので黙らせる）。mutation の行は対象数と**書き換えの予告**を兼ねる
- **#16 skill 2 点。** 手元の install も `postinstall` で落ちる（CI 側には既に案内があった）。
  step 6 が手元で `full` を回させるのに `--inPlace` の注意が無かった。CI の job の
  timeout 確認も足した

### 0.19.3: 同じ作業ツリーで mutation を二重に走らせない（2026-08-11）

**このセッション中に実際に踏んだ事故から。** 別のセッションが同じリポジトリを触っていて、
Stryker の `--inPlace`（実行前に全体を退避し、終了時に書き戻す）が衝突し、こちらの
未コミットの編集が丸ごと消えた。DESIGN の「mutation は CI でしか走らせないので作業ツリーは
使い捨て」という前提が、`doctor` と手元 `full` を足したことで崩れていた。

一時ディレクトリの印（pid）で二重起動を止める。印を消さずに死んだ場合（`kill -9`・電源断）は
残骸として無視するので、永久に止まることはない。

**当初考えた「作業ツリーが clean でなければ止める」は設計として誤り** — skill の種置きは
init が書いた未コミットの設定の上で `full` を回すし、普段の開発でも「直してから確かめる」が
普通で、正しい使い方を塞ぐ。危険なのは汚れたツリーではなく、同時に 2 つ書き戻すこと。

コードにならない分の教訓: **書いたら先にコミットしてから `full` を回す。** 未コミットの
編集を Stryker の書き戻しに晒さない。

### 0.19.2: Stryker が置けない / 型を見る / mode を落とす（2026-08-11、#9 #10 #11）

h3 に 0.19.0 を入れた回の報告 3 件。**どれも `doctor` が見つけた** — 導入時に mutation が
一度も走らない穴（#8）を塞いだ直後に、その先で待っていたものが 3 つ出た形。

- **#9: 置けない位置に当たると実行ごと落ちる。** h3 は内部 API を `~` 接頭辞で隠すので
  メソッド名が文字列リテラルになる（`"~request"()`）。Stryker はそこに三項演算子を
  置けず、instrumentation ごと落ちる → 変異が 1 件も走らない。
  **上流の履歴を見ると 2020 年から 1〜2 年おきに別の構文で再発**していて（#2543 →
  #3253 → #3647 → #3702 → #4884、直近の #6149 は未修正）、構文ごとに直しては
  また出る形。エラー文が外すべき mutator を自分で名乗るので、gauntlet は
  **名指しされた 1 種類だけ外して 1 回だけ回し直し、外したことを出力に出す**。
  h3 のための特例ではなく、上流が全損したときの受け身 1 つ。
  併せて、落ちたときの窓（`lastLines`）がスタックで埋まって理由が消えていたのを直した
- **#10: vitest の typecheck が有効だと mutation が原理的に動かない。** `--inPlace` が
  書き戻す計装コードが tsc を通らないため。ラッパーで `test.typecheck` を切る。
  型は別ゲートで見ているので検査は弱まらない
- **#11: 実行ビットを落としていた。** 0.18.1 で直したつもりだったが、mode を控えるのが
  **変異対象だけ**だった。Stryker の退避・復元はプロジェクト全体に及ぶので、
  `bin/h3.mjs`（`src/**` の外）は戻らない。git が持つ 100755 の一覧
  （`git ls-files -s`）を控える形にした — 何を触るかを知らなくても過不足なく戻る

**この回で作業ツリーの事故も踏み、同じ回で塞いだ（0.19.3）。** 別のセッションが同じ
リポジトリを触っていて、Stryker の `--inPlace`（実行前に全体を退避し、終了時に書き戻す）が
衝突し、こちらの未コミットの編集が消えた。DESIGN の「mutation は CI でしか走らせないので
作業ツリーは使い捨て」という前提が、`doctor` と手元 `full` を足したことで崩れていた。

一時ディレクトリの印（pid）で二重起動を止める形にした。**当初考えた「作業ツリーが
clean でなければ止める」は設計として誤り** — skill の種置きは init が書いた未コミットの
設定の上で `full` を回すし、普段の開発でも「直してから確かめる」が普通で、正しい
使い方を塞ぐ。危険なのは汚れたツリーではなく、同時に 2 つ書き戻すこと。

事故の運用上の教訓（コードにはならない分）: **書いたら先にコミットしてから `full` を
回す。** 未コミットの編集を Stryker の書き戻しに晒さない。

### 0.19.1: 落ちた理由を default reporter から取る（2026-08-11、#7 の続き）

#7 の 0.19.0 対応は「`message` が空のとき」だけを見ていたが、h3 の実測で**空でないが
手掛かりが 0**という形が出た。同じ 1 回の実行を 2 つの reporter で見た比較が決定的だった:

| reporter | 見えるもの |
| --- | --- |
| default | `Error: Test timed out in 5000ms.` |
| json（gauntlet が読む側） | `Error: STACK_TRACE_ERROR` + スタックのみ |

vitest は timeout の理由を JSON 側で内部の置き換え文字列にする。判定は JSON のまま、
**理由が無いときだけ default reporter の出力から取る**形にした（`--reporter=json
--reporter=default` は併用でき、`--outputFile` は json 側にだけ効くことを実測で確認）。
両方出すと普通の失敗で同じ内容が 2 回並ぶので、`lacksReason` が真のときだけ添える。

`gauntlet list` も 1 行（`テストが落ちているため計測できません`）で終わっていたので、
落ちたファイルを名指しするようにした — `quick` / `full` なら直前の tests チェックが
並べているが、`list` にはそれが無い。

### 0.19.0: coverage の範囲 / 落ちた理由 / mutation の疎通（2026-08-11、#6 #7 #8）

h3 に 0.18.5 を入れた回の報告 3 件。どれも「赤の意味が伝わらない」形。

**#6: 測る範囲と vitest の coverage 設定が別物だった。** 範囲に入れたのに
`coverage.exclude` で消されたファイルは網羅率が永久に 0%。複雑度が閾値に触れた瞬間に
**テストを書いても直せない赤**になり、しかも「網羅率 N% で通ります」と到達できない
出口を案内する。実測で 2 つ分かった:

- `--coverage.include` は CLI が config を**上書きする**
- `--coverage.exclude` は**上書きできない**（config の除外が残る）

そこで `--coverage.include` に gauntlet の宣言を渡すようにした。対象は未テストでも
ゼロ行の項目として載るので、**それでも現れない = リポジトリの `coverage.exclude` が消した**
に絞れる。この差分をフル実行で名指しして落とす。CRAP の数字は変わらない
（現れないファイルは元から網羅率 0 として扱われていた）。

**#7: ファイル単位で落ちたテストの理由が 1 文字も出なかった。** h3 では並列負荷の
timeout で vitest 側の `message` が空になり、`0 / 1668 件が失敗` の下に名前だけが並んだ。
切り分けに手で 10 回近く回すことになったとの由。2 つ直した:

- 理由が空なら `npx vitest run <file> で確認してください` を出す（名前だけにしない）
- assert が 1 つも落ちずにファイルが落ちる形では失敗数が 0 になるので、
  `2 ファイルがテストを実行できませんでした（1668 件中の失敗は 0 件）` と数え直す

**#8: 導入完了時点で mutation が一度も走っていなかった。** 差分から対象を決める以上、
設定ファイルだけの導入コミットでは必ず「変異対象 0 ファイル」になる。道具が揃うことは
0.18.1 で確かめているが、**その道具が対象リポジトリの vitest を起動できるか**は別。
Stryker の `--dryRunOnly`（CI での確認用に用意されている）を使う `gauntlet doctor` を足した。
変異は作らず初回のテスト実行だけを行う。gauntlet 自身で 4.4 秒。

probe を 2 つ目（テスト付き）に増やす案は採らなかった — 手順が増える上に、
baseline に消えたファイルの記録が残りうる。確認したいのは疎通だけで、Stryker が
そのためのモードを持っていた。

### 0.18.5: 0 件マッチの include と `skills update`（2026-08-11、tepshq/gauntlet#4 / #5）

**#4: 綴りを誤った include が永久に咎められなかった。** `--include='src/**/*.ts,testt/**/*.ts'`
は 0.18.4 でも緑になる（`testt/**/*.ts` は 1 件もマッチしないので `reviewIncludes` の候補に
入らず、`src` が生きているので対象 0 にもならない）。書き手の意図は #3 の
`--include='src/**/*.ts,lib'` と同じで、実害も同じ「半分が測られないまま緑」。

**落とさず、scope に出す**形にした。落とせないのは、0 件マッチが「今このリポジトリに無い」
としか言っていないため — `src/**/*.tsx` を落とすと、**最後の .tsx を消しただけで無関係な赤**が
出る（環境で答えが変わる方向）。ディレクトリを指す書き方が落とせるのは、どのリポジトリでも
ファイルを連れてこないと言い切れるから。この非対称が判定の分かれ目。

```
✓ crap (0ms)  触った関数 11 / 測る対象 242 関数（23 ファイル）
  source.include の `testt/**/*.ts` は 1 件もマッチしていません（意図した書き方なら無視してください）
```

判定は `reviewIncludes` の 1 か所のままで、返す型が `{ dead, unmatched }` になっただけ。
`formatResult` は scope の 2 行目以降も違反と同じ段に入れるようにした。

**#5: skill の更新手段が `npx skills update` ではいけない。** `~/.agents` があると実体を
`.agents/skills/`（多数の agent が共有する置き場）へ移して `.claude/skills/` を symlink に
するので、**追跡済みの `SKILL.md` が「削除」扱い**になる（h3 で 6 行の変更が 324 行削除に
化けた）。`update` のオプションは `-g` / `-p` / `-y` だけで、置き場所を指定する `-a` を受けない。
SKILL.md と README の追随手順を
`npx skills add tepshq/gauntlet -a claude-code -s gauntlet-setup -y` に差し替えた。
`--copy` は書かない — 使い捨てリポジトリで 3 手順とも実測した結果、**`-a` があれば
`--copy` は何も変えない**（`add` は実ディレクトリ、`update` が symlink、`add` で実ディレクトリに戻る）。
振る舞いを変えない指定を手順に残すと、読む側に「これが要る理由がある」と誤解させる。

### 0.18.4: 直し方を決め打ちにしない（2026-08-11、#3 の続き）

0.18.3 の案内が `src/**/*.ts` と言い切っていた。`lib/` にソースを置くリポジトリには
**当たらない助言**になる。パターンから候補（`<dir>/**/*.ts`）を組み立て、**実際に当てて
ソースに届いたときだけ**「これに直してください」と言う形にした。届かなければ
（gitignore された生成物に当たっているなど）当たっている先を言うだけにする。

`init --help` と skill の例も `<dir>` の形にした。この修正自体の変異で 2 件生き残り、
どちらも**直し方の作り方そのもの**だった（`src/utils/` のような末尾スラッシュで、
途中の `/` を消して `srcutils/` を案内してしまう形）。

### 0.18.3: `--include=src` が対象 0 の設定を書いていた（2026-08-11、tepshq/gauntlet#3）

0.18.2 を h3 に入れ直したときの報告。前回は `--include='src/**/*.ts'` を渡していたので
当たらず、今回 `--include=src` と打って当たった。

`src` は glob として**成立する**（ディレクトリ `src` 自身にマッチする）が、中の `.ts` は
1 つも連れてこない。`init` は 3 行とも「作成」で終わり、`quick` が
「実在しないパスを指している可能性があります」と言う — **`src` は実在するので、
案内どおり綴りとパスを疑って探すことになった**。

`init` 側でも止める案（報告の提案 1）は採らなかった。**同じ性質を守る仕組みを 2 つ置かない**
（CLAUDE.md）。1 つ目（`measurementFaults`）は今回ちゃんと落としていて、破れた証拠が無い。
壊れていたのは**診断の中身**なので、そこを直した。代わりに 1 つ目の守備範囲を広げてある:

- **include をパターン単位で見る。** 「何かにはマッチしているのに、ソースを 1 つも
  連れてこない」ものを名指しし、直し方（`src/**/*.ts`）を添える
- **他の include が生きていても落とす。** `--include='src/**/*.ts,lib'` のように片方だけ
  死んでいると、報告の提案 1（対象の合計が 0 なら止める）では捕まらず、範囲が黙って
  狭いまま緑になる。これが「範囲が狭いまま緑」の実在する経路
- **1 つもマッチしない include は咎めない。** `src/**/*.tsx` のような先回りは害が無い
- 黙って `src/**/*.ts` に正規化する案（提案 3）も採らない — 書いた値と config の値がずれる

skill と `init --help` の文言も直した（3 で人に見せるのはディレクトリ名でよいが、
`--include` に渡すのはファイルを名指しする glob だと 4 で明示する）。

### 0.18.2: h3 導入完走後の報告 6 件（2026-08-11、tepshq/gauntlet#2）

0.18.1 で h3 への導入が**最後まで通った**あとの報告。ゲートが噛む状態まで行ったので、
今度は**導入の手順と出力の読めなさ**が出た。

- **`--config.minimumReleaseAge=0` は有害だった（0.18.1 で私が入れた）。** 解決時にしか
  効かないので、書き上がった lockfile が以降**すべての pnpm コマンド**で弾かれる
  （`pnpm run` すら通らない）。使い捨てディレクトリで両方向を再現した:
  フラグ有り → `ERR_PNPM_MINIMUM_RELEASE_AGE_VIOLATION`、フラグ無し → pnpm が
  `minimumReleaseAgeExclude` を書き、その後は全部通る。**除外行を書かせるのが唯一の正しい
  経路**で、「触るファイル 3 つ」を守ろうとしてリポジトリを壊す方を既定にしていた。
  版ピンが積み上がる件・ワイルドカードにするかの判断・間接依存
  （h3 では stryker 経由の `update-browserslist-db`）・pnpm 11 の overrides の置き場所も skill に書いた
- **probe の後始末が通らなかった。** フックは呼び出し全体を実行前に止めるので `git add` も
  走っておらず、ステージは空。`git restore --staged` は必ず落ちる → `rm` だけにした
- **probe が通ってしまった場合の `git reset --hard HEAD~1` が設定を消す。** init の 3 ファイルは
  step 6 まで未コミットなので、`git add -A` の probe commit に巻き込まれる。`--hard` は
  コミットの中身に関わらず未コミットの変更を捨てるので、ステージの仕方に関わらず危険。
  → probe だけを `git add` し、取り消しは mixed reset に
- **`測る対象 411` が突き合わせられなかった。** 範囲を決めるとき人間が数えるのはファイル
  （h3 では 50）。report は「関数を持つファイル」と「関数が無くて外したファイル」の両方を
  持っているので、追加の走査なしで `411 関数（50 ファイル）` にできた
- **baseline の中身に辿れない。** `{ "crap": 35 }` から「どの 35 件か」を知る手段が無く、
  h3 側は手で coverage を取り直して**未参照のまま残っていた関数 2 つ**と
  **網羅率 0 の公開 API 1 つ**を見つけた。gauntlet が検出していたのに出力からは見えなかった
  → `gauntlet list`（ゲートではない。悪い順に全部、件数を切らない）
- **閾値 8 の逆算が書かれていない。** coverage 100% で `CRAP = CC` なので CC 9 以上は到達不能。
  h3 の違反 35 件のうち 25 件が coverage 90〜100% で、テストを足しても 1 件も減らない。
  → 表を足すのではなく、**違反 1 行ごとに次の一手を書く**（`→ 網羅率 51% で通ります` /
  `→ 複雑度 9 以上はテストでは通りません。関数を割ってください`）。読み手に逆算させない

`list` の実装が初回から自分の CRAP に引っかかった（CC 4 / 網羅率 0 の殻）。判断を
`violatorReport` に出して殻を CC 1 にした — 閾値を緩めずに直せる形が実際にあった 1 例。

### 0.18.1: h3 のパイロット報告 6 件（2026-08-10）

h3 で `/gauntlet-setup` を最後まで走らせたエージェントからの報告。**pnpm リポジトリで
初めて回した回**で、npm 前提の穴が 2 つ出た。

- **［高］pnpm では mutation が必ず落ちる。** Stryker の既定 `plugins: ["@stryker-mutator/*"]`
  は自分自身の隣を readdir する（npm / yarn のフラットな `node_modules` 前提）。pnpm の
  分離レイアウトではそこに api / core / instrumenter / util しか無く、vitest-runner は
  別の仮想ストアに置かれるので**原理的に見つからない**。
  → 対象リポジトリ基点の `createRequire().resolve()` で実体を解決し、`plugins` に
  絶対パスで渡す（絶対パスは glob に入らずそのまま読まれる）
- **［高］その故障が導入作業では見えない。** 種置きの時点では差分にソースが無いので
  `✓ mutation (0ms) 変異対象 0 ファイル` で緑になり、**src を触る最初の PR まで隠れる**。
  → 対象 0 でも道具の解決だけは先に行う（`requireMutationTools`）。道具の有無は
  対象の有無と無関係
- **［中］正しい CRAP 違反が「coverage 設定のずれ」に化けていた。** `quick` の全関数 0% は
  「変更したファイルにテストが届いていない」でも起きる（新規の未テストファイルを 1 つ
  足した差分がまさにそれ）。fault が出ると閾値の判定を飛ばすので、**関数名もスコアも
  消える**。h3 の coverage は健全（373/412 被覆）だった。
  → fault 判定はフル実行（`full`）にだけ当てる。`quick` のこの条件には識別力が無い
  （hono で入れた `触った関数 > 0` の条件が、まさにこれを踏んでいた）
- **［中］`full` が実行ビットを落とす。** `--inPlace` の書き戻しで mode が失われ、
  `bin/h3.mjs` が 755 → 644 に。→ 変異対象の mode を控えて `finally` で戻す
- **［低］skill の「版ずれ」の説明が実態と違った。** 原因はキャッシュではなく pnpm の
  `minimumReleaseAge`（既定 24 時間の供給網対策）。公開日の差も「何ヶ月」ではなく 31 時間。
  → skill / README を実態に直した。**ここで入れた `--config.minimumReleaseAge=0` は
  0.18.2 で撤回した**（下記）
- **［低］pnpm の workspace root では `-w` が要る**（無いと `ERR_PNPM_ADDING_TO_ROOT`）

### 0.17.1: `init --help` が範囲を既定値に戻していた（2026-08-10）

h3 で `/gauntlet-setup` を走らせたエージェントの報告から:
「init はヘルプフラグを持たず、その場で実行されてしまいました」。

**0.17.0 で私が入れた回帰**。`parseInitOptions` が「`--` で始まる引数が 1 つでもあれば
範囲を指定して呼ばれた」と判定していたので、`init --help` が「範囲指定あり・
ただし `--include=` は無い」と読まれ、**既定値で config を上書き**した
（`lib/**` → `src/**`。使い捨てリポジトリで再現）。同日 2 回直したデータ損失と
同型のものを、別の扉から作っていた。

- `helpRequested`（`--help` / `-h`）を**実行の前に**見る
- **知らないフラグは exit 2 で止める** — `--includes=`（打ち間違い）を素通しすると
  「指定したつもり」で既定値が書かれる。`main.ts` の usageError（知らないコマンドを
  素通しさせない）と同じ原則を、init のフラグにも適用した
- `INIT_USAGE` に指定できるフラグとフラグ無しの意味を書き、止めるときに添える

**h3 で観測されたもう 1 つ**: h3 に `@teps/gauntlet@0.13.0` が入り、その 0.13 の init が
skill を 0.13 世代で上書きしていた（mtime で確認: 20:45 に skills add が 0.17.0 の正本を
配置 → 20:47 に 0.13.0 の init が上書き）。**古い init が skill を巻き戻す**という構造の
実害で、0.17.0（init は skill を書かない）がまさにこれを止める変更だった。

### 0.17.0: 道具は npm、使い方は skills（2026-08-10）

ユーザーの「gauntlet はただインストールし、使うためのスキルを別途配布するのはどう？
npx skills add で入れられるように」から。**リポジトリを public にしてもらって**成立した
（private の間は GitHub 認証が要り、public npm 化で消したはずの認証問題が戻る）。

実地で確認したこと:

- `npx skills add tepshq/gauntlet` は **public 化した瞬間から動いた** — gauntlet 自身が
  dogfooding で置いていた `.claude/skills/gauntlet-setup/SKILL.md` を自動で見つけた
  （= init の成果物を配布する循環になっていた。正本を `skills/` に移して解消）
- 既定では `.agents/skills/` に実体 + `.claude/skills/` から symlink + `skills-lock.json`
  という 3 種の成果物が入る。**`-a claude-code` を付けると** `.claude/skills/` への
  実体コピー + lockfile だけになる（ユーザーの指摘。これで侵襲性がほぼ従来と同じ）
- lockfile は `computedHash`（内容ハッシュ）を記録。コミット SHA や版ではないので、
  「パッケージ 0.17.0 に対応する skill」という紐付けはできない → 版の一致は運用規律

変更:

- skill の正本を `skills/gauntlet-setup/SKILL.md`（254 行）に移した。それまで
  `init.ts` の中の 254 行のテンプレートリテラルで、diff が読めなかった。
  抽出は dist と突き合わせて**完全一致を確認**してから
- `init` は skill を書かない → 置くのは 3 ファイル（config / settings.json / .gitignore）
- **旧 skill の掃除（`removeLegacySkill`）も init から外した** — 自分が所有しない
  ファイルを消すのは境界違反。移行の後始末は skill の案内に移した
- skill の中身を検査するテストは、init の成果物ではなく正本のファイルを読む

**h3 を戻して分かった、この移行がまさに必要だった理由（同日）:**

h3 に残っていたのは **0.13 世代の skill**（`git config core.hooksPath .githooks` を
指示していた — 0.14 で捨てた手順）と `@teps/gauntlet: ^0.13.0`、`.githooks/pre-commit`。
今日 0.16.0 の `init` を叩いても、**古い skill が置き去りのまま**だった。
「init が skill を書く」形の drift が実地で出た形。

- `skills add` の配布は健全と実測: リポジトリに `.claude/skills/gauntlet-setup/`（0.14 世代の
  コミット済みコピー）と `skills/gauntlet-setup/`（0.17.0 正本）の 2 つがあったが、
  lockfile の `skillPath` は正本を指していた
- それでも**自分のリポジトリの化石を撤去**した — `.claude/skills/gauntlet-setup` を
  正本への symlink（git は mode 120000 で記録）にした。同名の古い文書は次に読む
  エージェントを誤らせる（duct #567 の教訓の再演）。symlink なら二度と drift しない

### 0.16.1: coverage provider の版を gauntlet が計算して言う（2026-08-10）

ユーザーの「これがもっともエレガントな解ですか？ インストールがこんなに難しい
パッケージは成立しないんじゃ？」から。skill と README が利用者に
\`@vitest/coverage-v8@$(node -p "require('vitest/package.json').version")\` という
呪文を書かせていた。

実測で分かった制約の連鎖:

- \`@vitest/coverage-v8@4.1.10\` の peer は **\`vitest: '4.1.10'\` の完全一致**（範囲ではない）
- **vitest 側は provider を peer 宣言していない**ので npm は必要性を知らず、助けようがない
- 版無しで入れると最新が来て **ERESOLVE で install ごと失敗**（vitest 3.2.7 の repo で実証）
- npm が既存 vitest に合わせて古い provider を選び直すことは**しない**（ユーザーの仮説を実測で否定）

**正解の版はリポジトリの中にある**（入っている vitest）ので、gauntlet が読んで
コマンドを組み立てる — Stryker で既にやっていた形に揃えた。provider が無ければ
\`quick\` / \`full\` が走る前に版込み・PM 込みの 1 行を出して止まる:

\`\`\`
coverage provider（@vitest/coverage-v8）が入っていません。次で入れてください:
  pnpm add -D @vitest/coverage-v8@3.2.7
\`\`\`

- 版は \`node_modules/vitest/package.json\` から読む（\`^3\` 指定でも実体は 3.2.7 なので、
  package.json の指定ではなく解決後の実物が要る）
- これまでは vitest の「MISSING DEPENDENCY」が下位ツールの生エラーとして漏れていた
- skill と README から呪文を削除。「gauntlet が出す 1 行を打つ」だけになった

### 0.16.0: init を「骨組みを置く」だけに戻す（2026-08-10）

0.15.1 の直後、ユーザーの「それは意味的に init じゃないよな」「繰り返し
gauntlet init する必要がなぜあるの？」から、**もう 1 つ同型のデータ損失**が出た。

- **バグ**: フラグ無しの `init` が測る範囲を既定値で上書きする。`lib/**` を測る
  リポジトリで、新しいフックを取り込もうと叩き直すと `src/**` に戻る（実証済み）。
  0.15.1 で直した `commands` 消失とまったく同じ形が、範囲そのものに残っていた。
  → **フラグ無しは既存 config に触らない**（出力は「変更なし」）。範囲を書き換える
  のはフラグを渡したときだけ。読めない config も残す（上書きすると手書きの範囲が
  消える。壊れた config は実行時に ConfigError で落ちるので黙って進まない）
- **件数表示を廃止**（`measuredFileCount` / `scopeLine` を削除）。skill の完了条件が
  既に `npx gauntlet quick` の `測る対象 N` で検算していて、init の件数は二重だった。
  「0 なら言葉で言う」「フラグの時だけ出す」と数回いじったが、**要らないものの
  見せ方をいじっていただけ**だったと分かって全部消えた
- 次の一歩の案内（`/gauntlet-setup`）は **config を新しく作った回だけ**。設定済みの
  リポジトリを更新した回に出すと雑音
- init の 3 つの呼ばれ方が出力で見分けられるようになった: 骨組み（作成 + 案内）/
  範囲を決める（更新）/ 移行（変更なし + 配線だけ更新）
- 教訓: **「なぜ 2 回叩くのか」という素朴な問いが、名前と実装の乖離を暴いた。**
  init が「初期化」と「範囲の設定」を兼ねていたので、後者の頻度で前者が暴発していた
- **CRAP が自分の設計違反を捕まえた**: 判断（フラグの有無・案内を出すか）を
  `main.ts` に書いたら `initCommand` が CC 3・網羅率 0 で **CRAP 12** で止まった。
  main.ts は「薄く保つ。判断はテストできる場所に」と自分で決めた場所。
  `formatInit` として init.ts に移し、`initCommand` は 1 行に戻った。
  副産物として `parseInitOptions` が**フラグ無しなら null を返す**形になり、
  「範囲の指定なし」が型で表現された（`drivenByFlags` は不要になり削除）
- mutation もまた案内文の後半を消して生き残った（部分一致のテストだったため）。
  0.15.1 と同じ指摘で、**メッセージは丸ごと固定する**を再確認して塞いだ

### 0.15.1: init の再実行が commands を消していた（2026-08-10）

h3 の導入直後、ユーザーの 2 つの問い（「settings.json は置き換え？」
「この表記は適切なんか？」）から**データ損失のバグ**が出た。「各ファイルに何を
しているか」を確かめたら、`gauntlet.config.json` だけ全上書きだった。

- **バグ**: `init` の再実行で手書きの `commands.typecheck` が消える。skill は
  「commands は手で書く」「範囲を直すたび init を叩く」と案内しているので、
  **案内どおりに使うと踏む**。teps のような 2 パス型チェックでは既定の
  `tsc --noEmit` に落ち、半分しか見ないまま緑になる（黙って検査が狭まる形）。
  README の「何度叩いても結果は同じ」も、手書き設定については嘘だった。
  → フラグで管理しないキー（`commands`）は既存から引き継ぐ。settings.json /
  .gitignore と同じ「マージする」振る舞いに揃えた
- **出力にファイルごとの動作を書く**: 4 ファイルで振る舞いが 3 種類（全上書き /
  マージ / 追記）あるのに平坦なリストで、読み手が「自分の settings.json は
  無事か」を判断できなかった。`作成` / `更新（既存の設定は残しました）` /
  `更新（6 行追加）` / `変更なし` を各行に出す
- **「対象外に TypeScript があります」を init から削除**（ユーザーの指摘 —
  「範囲は skill の中で確認・調整していくんじゃないん？」）。**証拠がある**:
  この報告は最上位ディレクトリしか挙げられず、duct のルート直下 `proxy.ts`
  （Auth0 の認証ゲート本体）の測り漏らしを捕まえられなかった — 存在理由の
  ケースそのもので機能しなかった。skill は 1 件ずつ分類するので上位互換。
  `scopeReport` は `measuredFileCount` に縮小（0 件の信号だけ残す）
- 読めない `settings.json` の生スタックトレースも ConfigError に。設定は
  **全部読んでから書く**ので、落ちた回は 1 ファイルも書かない
- **mutation が今回の変更に 4 件の生き残りを出し、うち 2 件が本物の検査不足だった**
  — `更新（N 行追加）` の N を検証していない（引き算が足し算に化けても通る）、
  エラーメッセージの助言部分（「コメントや末尾のカンマがあれば〜」）を検証して
  いない。後者はこの版で足したメッセージそのもので、**「メッセージは直し方まで
  言う」という今日の修正を、自分のゲートが検査不足として突き返した**形。
  残り 1 件は等価変異（`JSON.parse(null)` は null を返すので早期 return を
  外しても同じ）で、理由つきの disable コメントにした

### 0.14.0: quick の起動点を PreToolUse に集約（2026-08-09）

0.13 の git pre-commit を捨て、Claude Code の `PreToolUse` + `if` に移した。
**きっかけはユーザーの 2 つの問い** — 「`git config core.hooksPath` は絶対必要なのか」
「コーディングエージェントに結果を戻して欲しいから git の hook では足りないのでは」。
どちらも当たっていた。

調べて分かった一次情報（公式ドキュメント）:

| | 事実 |
| --- | --- |
| `PreToolUse` の `if` | **permission rule 構文でコマンドの中身を絞れる**。`Bash(git commit *)` で、先頭の変数代入を除き `&&` の各コマンドも `$()` の中も検査される |
| `matcher` | tool 名のみ（コマンドの中身は `if` の役割） |
| exit 2 の挙動 | stderr が「ツール呼び出しをブロックした理由」として Claude に返る |
| タイムアウト | 既定 600 秒。60 秒の検査は余裕 |
| `Stop` フックの穴 | **8 回連続ブロックでキャップ**。赤いまま通る（0.12 以前の方式が持っていた） |
| PreCommit/PostCommit フック | 公式に存在しない（feature request は「実装予定なし」で close） |

**自前のコマンド解析を書いて、捨てた。** `if` を知る前に `commit.ts` を実装したが、
`segmentMakesHistory` が CC 12 で自分の CRAP ゲートに引っかかった。公式の `if` の方が
正確（`$()` の中まで見る）で、gauntlet 側のコードは 1 行も要らなかった。
**ゲートが「この関数は危ない」と言ったのは正しく、その先に「そもそも書く必要が無い」があった。**

実地検証（gauntlet 自身、設定を書いた直後のセッションで即座に効いた）:

- 緑: `gauntlet quick: pass (1720ms)` の後にコミット成立
- 赤: 未テストで CC 6 の関数を足したら
  `PreToolUse:Bash hook error: [npx gauntlet quick]: ... CRAP 42.0 (> 8)` で
  **コミットが実行されず HEAD が動かなかった**。理由はエージェント（私）に直接届いた

**残る代償**: 人間がターミナルで直接打つコミットは検査されない。§1 の前提
（コードを書くのはエージェント）で受け入れ、git の pre-commit 併置は
「破られた証拠が出てから」に回した。

### 一巡したら決めたいこと

**~~mutation が「0 件検査して緑」と「検査して問題なし」を区別していない。~~ 解決（0.0.10）。**
判定は足さず、全チェックが「何を見たか」を必ず出す形にした（`CheckResult.scope`、DESIGN §2）。
`✓ mutation (0ms)  変異対象 0 ファイル` と読めるので、沈黙が主張になった。

**~~重複（duplication）のゲートが無い。~~ 決着（2026-08-08）: 0.11.0 で実装した。**
「検討して外したもの」に入っていない考え落とし状態だったので、実測してから決めた。
jscpd（min-tokens 50）で gauntlet 5 クローン / **duct 377 クローン・重複行 3.17%**
（`composition-agent` と `identity-agent` の route に約 100 行の丸写し等）。
一度「持たない」と決めた直後に、「シンプルに収まるなら」の条件付きで採用に反転 —
repo 全体の重複トークン数 1 つを CRAP と同じ ratchet に載せる形なら追加機構が
ほぼゼロで済むため（DESIGN §3）。実装は下の 0.11.0 の節。

**mutation の判定が run 間でブレる（hono で実測）。** 同じコード・同じテストの 2 回の `pr` で、
差分が触れていないファイルの Survived が 9 → 10 になり ratchet が落ちた。並列実行下の
timeout 揺れが疑わしい。ratchet は「増やさない」を厳密比較で課しているので、
ブレ 1 件がそのまま赤になる。許容幅・リトライ・timeout 隣接の扱いのどれで受けるかは未決。
→ 追加実測（2026-08-08）: gauntlet 自身で同一条件 8 連続実行して**ブレ 0/8**。
0.10.1 の範囲修正（膨張の解消）で、触っていないファイルが範囲に同乗する面積も縮小済み。
再発時の対処案: baseline を件数から名簿（file:行:変異種）に変え、新顔だけ再現確認する。
チームの repo の実運用で再発するまで機構は作らない。

**~~main 直コミット運用では mutation の記録が締まらない~~ 決着（2026-08-12）: 機構は足さない。**
締まる条件が「clean なツリー」と「変異対象に入っている」の両方で、直コミットでは
両立しない（測れるのはコミット前、clean になるのはコミット後で差分が空）。
config.ts を 16 → 1 にした直後の `full` で、CRAP は締まったが mutation は 16 のまま残った。
記録が**緩い方向**にずれるだけなので緑の意味は壊れず、flaky でも gameable でもない。
→ **同日、feature ブランチで窓の存在を実測**（`baseline-conflict-resolve`）。コミット後の
clean なツリーで `full` を回すと差分の起点が `origin/main` なので変異対象は空にならず、
12 ファイル分が一度に締まった（config.ts 16 → 1、guard.ts 2 → 0、旧形式 12 ファイルに
母数と打ち切りが入った）。**PR 運用では噛み合う**ことが確認できたので、
`full --record` のような明示の記録回は作らない。gauntlet 自身の main 直コミット運用が
唯一の当事者で、それは「ブランチを切って PR にする」で解ける。

**~~integration テストを gauntlet が見るべきか~~ 決着（2026-08-08）: どの tier も見ない。**
実行・coverage・mutation から束で除外した（DESIGN §2）。Stryker には project フィルタが
無いので、リポジトリの vitest 設定から `integration` project を濾した一時設定を生成して
渡す形にした。**duct への影響**: 次に gauntlet を更新すると全体 CRAP が 761 → 772 に
増えるため、baseline の明示的な後退更新（フラグ + PR 説明）が一度だけ要る。
判断の根拠になった実測（duct、25 ファイル・134 テスト）:

| | 全テスト | integration なし | 差 |
| --- | --- | --- | --- |
| テスト | 7355 | 7221 | 134 |
| 覆われている関数 | 5979 | 5947 | **32**（全て `lib/`） |
| CRAP > 8（全体） | 761 | 772 | **+11**（+1.4%） |

integration でしか覆われていない関数は 6669 中 32（0.5%）で、`lib/import/session.ts`・
`lib/destructive/audit.ts`・`lib/diff-preview/build-input.ts` 等の DB 境界に集中。
新たに違反になる 11 件の多くは `jsonOrNull` や `uniqueProductIds` のような
ユニットテスト可能な純粋ヘルパーで、要求される圧力は「DB をモックしろ」ではなく
「ヘルパーに単体テストを書け」に近い。hue は integration を持たず、teps は単一 project
（e2e は Playwright で vitest の外）なので、影響があるのは duct と今後の導入先だけ。
なお hono の変異対象膨張（27 ファイル）は integration project では**なく** fan-in の大きい
ユニットテストが原因なので、この除外では解決しない — 別の問題として残っている。

### 7. パイロット投入

- **hue** — 機構そのものの検証。既に 87.8% カバー済みなので、テストを書く作業と混ざらない
- **teps** — 規模（2222 テスト）と coverage provider 導入の検証
- **duct** — まず `npm install` が通るかの確認から

**完了条件** — hue で実際の PR が 1 本 gauntlet を通ってマージできる。
3 リポジトリそれぞれで `turn` の実測時間を記録し、上のベースライン表に追記済み。

### hue（完了、`adopt-gauntlet` ブランチにコミット済み）

旧 `@tepshq/gauntlet` 0.8.0 を完全に除去（`.gauntlet/` 5 ファイル・`gauntlet.config.mjs`・
依存・scripts 5 本）。`lint` と `depcruise` は旧 gauntlet 経由で eslint と dependency-cruiser を
呼んでいただけなので、下地のツールを直接叩く形に戻した。

| | 実測 |
| --- | --- |
| `turn` | **5.36 秒**（typecheck 0.6s / tests 4.7s / crap 0s） |
| `pr` | **142 秒**（うち mutation **137 秒**） |
| 測る対象 | 24 ファイル（`src` + `bin`） |
| CRAP 違反 | 1（seed 済み） |
| mutation 生き残り | 590（seed 済み。`server.ts` 137 / `stream.ts` 103 / `runner.ts` 68 が上位） |

`turn` の 4.7 秒はテスト実行。設定ファイルを変更しているため vitest が全テストを選んでいる状態で、
ソースだけの変更ならもっと短くなる。

**CI（0.0.2、差分スコープ前）: 成功。ただし 948 秒（うち mutation 932 秒）。**
GitHub Packages の認証経路がこれで検証できた（パッケージ設定の Manage Actions access で
`tepshq/hue` に Read を付与した後）。同時に、mutation の範囲を絞る修正が
必要だったことの裏付けにもなった — 24 ファイル全部を変異させて 15.5 分かかっている。

パイロットで見つけて直したもの（いずれも hue で実際に踏んだ）:

- **`init` が冪等でなかった。** 測る範囲を直すために 2 回叩いた結果フックが二重登録され、
  毎ターン gauntlet が 2 回走る状態になっていた。同じ内容は二度足さないようにした（0.0.2）。
- **CI の workflow に GitHub Packages の認証が無かった。** `npm ci` の時点で必ず落ちる。
- **CI では `git merge-base HEAD main` が解決できない。** checkout は対象ブランチしか
  ローカルに作らないため。`origin/main` へフォールバックする。
- **`--inPlace` の Stryker が `stryker-setup-*.js` と `.stryker-tmp/` を残す。**
  前者は実行後に消し、後者は `init` が `.gitignore` に足す。

- **`npx stryker` が対象リポジトリに Stryker が無いと npm から非推奨の別パッケージを取ってくる。**
  `node_modules/.bin/stryker` を直接見て、無ければ導入コマンドを示して止める。
- **下位ツールのエラーを握り潰していた。** レポートが出ていないときは Stryker の出力を添えて落とす。
- **`init` が測る範囲を黙って推測していた。** 対象外に TypeScript がある場所を出すようにし、
  skill を「エージェントがリポジトリを読み、理由つきで提案し、ユーザーの同意を得てから
  `init` を叩く」流れに書き直した（DESIGN §4）。tsconfig の `include` から機械的に導けるかを
  3 本で試したが、hue は正解・teps と duct は不正解で、判断が要ることが確認できた。

### duct 本導入（tepshq/duct#567 — **CLOSED。マージされなかった**。2026-08-09 の実測）

0.12.0 で main から入れ直した、実運用に入れるための PR。過去 3 本（#559 / #563 / #564）は
すべて検証で閉じたが、今回は**ローカルゲートが実際に噛むところまで確認**した。

**2026-08-12 に確認: この PR は閉じられ、duct は gauntlet を導入していない。** duct の main には
`gauntlet.config.json` も `gauntlet.baseline.json` も無く、`.claude/settings.json` に gauntlet の
配線が入ったこともない。`node_modules/@teps/gauntlet` だけが残っているので `npx gauntlet
--version` は 0.21.1 と答えるが、**版が答えることは導入の証拠にならない**。下の実測値は
PR 上のもので、今も有効なのは数字だけ。

| | 実測 |
| --- | --- |
| `quick`（pre-commit） | **60 秒**（typecheck 1.9s / テスト 7213 件 57s / CRAP 6665 関数） |
| `full`（CI、認証なし・DB なし） | **15 分 6 秒** |
| `ci.yml`（DB あり、integration 担当） | 12 分 33 秒 |
| 測る対象 | 778 ファイル / 6665 関数 |
| baseline の種 | CRAP 違反 773 / 重複 29,484 トークン |

- **起動点は pre-commit**（DESIGN §2 の「遅いリポジトリ」の形を初めて実地で採用）。
  `.githooks/pre-commit` をコミットし、各開発者が一度だけ `git config core.hooksPath .githooks`。
- **ゲートが噛むことを実証**: 未テストで CC 8 の関数を足したら `CRAP 72.0 (> 8)` で
  コミットが拒否され、HEAD が動かなかった。導入コミット自身も pre-commit を通っている。
- **Vercel が緑**（public npm 化の効果。8/8 は private パッケージで 401 だった）。
- **DB 依存の判定は名前ではなく実行で確定**させた。名前に "integration" を含む 6 ファイル
  （`compose-integration.test.ts` 等）は DB 無しで通ったので宣言側に残し、逆に名前が
  合っていなかった DB 依存 4 ファイルを `*.integration.test.ts` に移した。

**この導入で見つけた gauntlet 側の不具合**: DESIGN のバージョニング節に、0.10.0 で
廃止した「`integration` 除外」「`integration` project 規約」が公開 API として残っていた。
導入作業でこの化石を読み、廃止済みの規約を復活させる提案をしかけた（ユーザーの指摘で発覚）。
**ドキュメントの化石は、次に読むエージェントを誤らせる**という実例。

### duct 第 2 期（tepshq/duct#564 — 全チェック緑を確認後、検証完了として close）

導入 PR は 3 本とも閉じた（#559 / #563 / #564）。duct の main に gauntlet は入っていない。
0.10.0 が public npm にあるので、本導入は main からいつでも数分でやり直せる
（`npx @teps/gauntlet init` → skill の対話。新規クローン通しで実測済み）。

0.9.0（integration 除外 + public npm）での導入 PR。gauntlet job は**認証もサービスコンテナも
無い最小 workflow**（checkout + setup-node 22 + `npm ci` + `npx gauntlet run --tier=pr`）で
**14 分 50 秒 pass**。依存は exact pin。baseline は 761 → 773 の明示的後退 2 回
（integration の coverage 剥離 32 関数 +11、隠れ DB テスト 2 本の移動 +1）。

ここで見つかったこと:

- **隠れ DB 依存の unit テスト 2 ファイル**（`lib/validation/__tests__/` の 2 本）。ファイル内に
  prisma への直接参照が無い間接依存で grep では見つからず、手元は Postgres が動いているので
  常に緑だった。**DB の無い CI が初めて炙り出した** — skill の「実行で確定させる」手順の
  検出漏れを補う形。`*.integration.test.ts` へリネームで解消。
- **Vercel の preview デプロイが private パッケージの 4 例目の被害者だった。** PR を作った
  瞬間に Vercel の `npm install` が 401。public npm への移行（0.9.0）で解消し、
  #563 / #559 が滞留していた一因もこれで説明がつく可能性が高い。

0.9.0 で main の新規クローンに一から入れる通し（2026-08-08、使い捨て clone）:
認証設定ゼロで install 2 秒 → 素の `init` が「測る対象 0」を警告 → 範囲指定で 837 ファイル →
初回 `turn` が DB 依存テスト 6 ファイルを正しく赤にする → integration project + リネームで
**DB 設定ゼロの環境で turn 61 秒緑**。ここで分かったこと:

- main には `*.integration.test.ts` の**名前だけ**あって project が無く、除外が効いていなかった
  （名前という規律は機構が無いと効かない、の実例）。
- **`lib/destructive/__tests__/audit.test.ts` は環境依存**。実 prisma で書き込み、失敗の
  *種類*で結果が変わる — CI（5432 に誰もいない = 接続拒否）は通り、別の Postgres が
  5432 にいる環境（認証エラー）は落ちる。~~#564 の CI が緑なのは偶然で、リネームが要る~~
  → 誤り。#564 の branch は導入コミット（f3a94aa）で**リネーム済み**だった。この実験は
  main の（branch 以前の）状態を見ていた。skill 駆動の導入が独立に同じ結論に達していた、
  という手順の検証としては良い結果。

### duct の実差分 turn は 48〜83 秒（2026-08-08、使い捨て clone で実測）

ソース 1 ファイルだけの差分での `turn`（`adopt-gauntlet-skill` を起点に、ファイル末尾へ
1 行追加して計測。0.10.0、`tests.projects` = node / dom）:

| 差分 | 選ばれたテスト | tests | typecheck | 合計 |
| --- | --- | --- | --- | --- |
| なし（config のみ） | 0 件 | 5.0s | 8.5s | **14.3s** |
| `lib/crypto/aes-gcm.ts` | 3,132 件 | 70.8s | 10.2s | **83.3s** |
| `lib/channels/index.ts` | 3,364 件 | 40.4s | 6.7s | **47.8s** |
| `lib/channels/rakuten/attr-name-resolve.ts` | 1,658 件 | 40.0s | 7.7s | **48.5s** |

（aes-gcm の 70.8s は最初の重い実行 = vitest の transform キャッシュが冷えた状態。
温まった 2 回目以降は 1.6〜3.4k 件で 40 秒前後に落ち着く）

分かったこと:

- **床が 14 秒ある。** typecheck（`tsc --noEmit` 全体で 7〜10s）+ vitest の起動と
  モジュールグラフ解決（選択 0 件でも 5s）。テスト選択をどれだけ絞っても、
  duct ではここより下がらない。
- **`--changed` の逆依存選択が、duct のアーキテクチャでは実質「全部」に近い。**
  直接 import が 2 ファイルしかない葉（attr-name-resolve）でも 1,658 件が選ばれる。
  validation の rules → registry、channels の barrel（`index.ts` の再輸出）が
  ハブになっていて、どこを触っても逆依存の閉包が数千テストに膨らむ。
  hono（実差分でテスト 17 件・2.2 秒）との差はスイートの大きさではなく**依存の形**。
  なお channels の barrel は f03 の設計文書が定める **public boundary**（外から内部を
  直接 import してはならない）なので、解体は機械的な作業ではなく設計判断になる。
- **coverage は犯人ではない。** 同じ 1,658 件選択を coverage なしの素の vitest で
  回して 37.7 秒（coverage ありは 40.0 秒）。gauntlet が上乗せしているコストは
  誤差の範囲で、遅いのは選ばれたテスト集合そのもの。
- **テスト本体の大半は「本物のタイマー待ち」。** vitest の内訳（worker 累積）は
  transform 3.6s / setup 6.4s / collect 39.6s / **tests 70.8s** / environment 27.7s。
  1〜2 秒かかるテストは `withRetry`（`lib/channels/shared/retry.ts`、
  `baseDelayMs: 200` の指数 backoff、実 `setTimeout`）を fake timers なしで
  待っている（`yahoo/client.test.ts` に `useFakeTimers` が 0 件、実測 1004ms /
  2003ms のテストあり）。ほかに jsdom 構築 27.7s、テストファイル import
  （collect）39.6s — barrel 経由の巨大な import 閉包がここにも効いている。
- Stop フックとして毎ターン 50〜80 秒は成立しない水準。速くする経路は効果順に
  (a) duct 側でテストの実タイマー待ちを排す（fake timers か sleep 注入。
  スイート全体と CI も速くなる）、
  (b) duct 側 `commands.typecheck` を incremental にする（8.5s → 1s 台、1 行）、
  (c) duct 側で barrel / registry の境界を per-channel に割って閉包を狭める
  （根本策だが public boundary の設計判断）、
  (d) gauntlet 側でテストを間引く — 緑の意味が差分サイズで変わる（flaky）ので
  やるなら DESIGN の判断。(a)(b) で 15〜25 秒圏、10 秒を切るには (c) まで要る見込み。

**(a)+(b) を使い捨て clone で実際に入れて再計測（同日）: 48.5 秒 → 30.0 秒。**
(a) は main 起点の PR にした — **tepshq/duct#565**（setup 1 枚 + config 1 行。
main での実測: `lib/channels` 35.24s → 1.42s、node project 全体 38.5s → 16.8s、
pass / fail / skip は前後で完全一致）。(b) は **gauntlet の既定にした** —
`DEFAULT_TYPECHECK` を `tsc --noEmit --incremental` に変え、`init` が
`*.tsbuildinfo` を .gitignore に足す。診断は変わらず速さだけ変わり、
権威ある判定の `pr` は CI で必ずコールドなので緑の意味も変わらない。
導入済みリポジトリはバージョンを上げるだけで効く（duct の本導入は
init が生成する config がそのまま既定を使う）。以下は clone 上の実験の記録:

- 実タイマーの主犯は retry ではなく **rate-limiter だった**（Yahoo は
  `capacity: 1, refillPerSecond: 1` = 1 QPS。テストが 2 リクエスト目で 1 秒、
  3 つ目で 2 秒、実時間で待つ。実測 1004ms / 2003ms と一致）。retry も
  rate-limiter も clock / sleep は**最初から注入可能に作られていて**、使われて
  いないだけ。vitest の setup 1 枚で仮想時計（sleep が仮想時刻を進める）を
  注入したら、`yahoo/client.test.ts` は 40 テストで 167ms になり、
  **1,658 件全部緑のまま** tests が 40.0 → 23.2 秒。
- incremental typecheck は床では 8.5 → 1.9 秒だが、**実差分では 6.2 秒までしか
  下がらない** — 変更ファイルの依存元を再検査するので、barrel 経由で閉包が
  膨らむ問題が typecheck にもそのまま効く。
- 残り 30 秒の内訳（worker 累積）: collect 44.6s（テストファイルの import =
  ハブの閉包コスト）/ tests 30.7s（実計算、~19ms/件）/ environment 24.6s（jsdom）。
  実タイマー待ちはもう見えない。**10 秒を切るには (c) が必要**という見立ては
  実測で裏付けられた — (c) は選択数・collect・incremental typecheck の
  再検査範囲、3 つ全部に同時に効く。

**(c) の正体を特定して提案化 — tepshq/duct#566（同日）。**

- barrel は犯人ではなかった。`lib/channels/index.ts` は型・hooks・registry 関数
  しか re-export しない薄い境界で、registry も空登録 + 動的解決。**ハブは
  `bootstrap.ts`**（全チャネル client を静的 import）で、lib 層の 21 ファイルが
  これを import している。しかも f03 design.md（2026-06-11 実装同期）に記録された
  **意図的対策** — Next.js は instrumentation と Server Action を別モジュール
  グラフにコンパイルし、モジュールスコープの registry シングルトンが分裂して
  "Unsupported channel" になる罠への対処。
- **vitest の `--changed` は動的 import の辺も数える**（duct clone に合成
  2 ファイルを置いて実測）。「bootstrap の中身を lazy にする」案は選択数に
  効かないと確定。
- 提案は「registry シングルトンを globalThis へ」— グラフ分裂という罠の
  根本原因を消し、lib 層の bootstrap import を不要にする。公開 API と spec の
  境界は不変（duct オーナーの意向: 実装済み spec は触らない）。要検証は
  Vercel cold start での instrumentation 登録の可視性。

**globalThis registry の spike 実測（同日）— 予想は外れた（#566 に追記済み）。**

- 実装は成立（tsc 緑、node 5,533 / 5,534 pass。1 件は「bootstrap を呼ぶこと」を
  検証する旧契約テストで期待を更新）。だが選択数は attr-name-resolve で
  **1,658 → 1,658（変化なし）**、yahoo/client で 1,398 → 1,243（−11%）。
- yahoo/client 変更で選ばれる 120 テストファイルの内訳: app/products 16 /
  lib/bulk-update 10 / components/settings 10 / components/products 9 /
  lib/diff-preview 8 / lib/sessions 7 … と**ほぼ全 feature に分布**。閉包の
  支配要因は単一ハブではなく、UI（pages / components）→ Server Actions →
  feature service → channel 内部という **feature レベルの編み込み**。
  単一ハブの切断では崩れない。
- 教訓: **実差分 turn ~25〜30 秒（仮想時計込み）が、選択の衛生で到達できる
  下限に近い**。duct で 10 秒圏に入れる残りの選択肢は (1) 30 秒を受け入れる
  (2) Stop フックを外し pr のみ運用 (3) gauntlet 側で「予算つき turn」を設計
  （選択が閾値超のとき範囲を絞り、絞ったことを scope に明示 — 緑の意味が
  変わるため DESIGN 判断）。globalThis registry 自体は罠の解消 + ensure
  呼び出し 6 箇所の削除という簡素化として価値が残る。
- **「ファイルごとの固定費」側のレバーも同日に測り切った。** テスト本体は
  ~19ms/件で健全。遅さは固定費（collect = import ~300ms/ファイル + jsdom +
  setup）× 選択ファイル数。`isolate: false` は機構としては効く（collect
  36.2→15.2s、environment 22.3→5.8s）が duct のテスト規約（per-file の
  vi.mock・モジュール状態）と非互換で **425 テスト失敗**。jsdom → happy-dom
  は probe 選択で誤差（14.9→14.3s。フルスイート 7,210 件は happy-dom で全緑）。
  **結論: duct の実差分 turn は「編み込み × 隔離前提の固定費」の積で決まり、
  安いレバーは出尽くした**（#566 に全実測を記録）。

### duct 第 1 期（`try-gauntlet` ブランチ / tepshq/duct#563 — #564 に置き換えて close 済み）

**唯一、リポジトリの持ち主が導入ガイドに沿って自分で入れた例**（skill はまだ実地で
試されていない — 開発者が読んだのはアーティファクトのガイド）。こちらが用意した
`adopt-gauntlet-v2`（#559）は使わず閉じた。導入手順そのものの検証がここで取れる。

測る対象は `lib` / `components` / `app` の `.ts` + `.tsx`（`src/` は無い）。

| | 手元 | CI |
| --- | --- | --- |
| `pr`（0.0.10、差分にソース無し） | 68〜82 秒 | 523〜791 秒 |
| うちテスト（7355 件） | 52〜66 秒 | 463〜694 秒 |

**注意: 3 本のパイロット全部で、これまでの緑は「触った関数 0 / 変異対象 0」。**
差分が設定とバージョン上げだけなので正しいが、ゲートが実際にソースを検査した緑は
まだ 1 つも無い（0.0.11 の検証で一時変更を作って mutation が判定を出すことだけは確認済み）。
各リポジトリで実際にソースを触る最初の PR が本当の試験。
→ 「実際にソースを検査した」実行そのものは hono の再生実験（下）で初めて取れた。
チームのリポジトリでの実 PR はまだ。

duct で見つけて直したもの:

- **`--exclude` の glob は vitest の `projects` に伝わらない。** 統合テストが `turn` でも走っていた。
  project 名で `--project=!integration` を指定する形に変えた。project を使っていない
  リポジトリでは無害なので、1 つの仕組みで両方に効く。
- **vitest はテストが落ちると coverage を書き出さない。** `coverage-final.json を読めません` が
  出て、本当の原因（テストの失敗）が見えなくなっていた。落ちていたら coverage を読まない。
- **`git ls-files` は非 ASCII のパスを 8 進エスケープする。** 日本語ファイル名で `ENOENT`。
  全 git 呼び出しに `core.quotePath=false` を付け、読めないファイルは 0 行として扱う。
- **`coverage.include` のずれを黙って緑にしていた。** 既定の `src/**/*.ts` のまま入れると
  duct には `src/` が無いので対象 0 件で緑になる。`measurementFaults` を入れた（0.0.8）。
- **baseline が履歴に無いままだった。** `pr` を CI でしか回していないので種が捨てられ、
  ラチェットが一度も噛んでいなかった。種を置いた回は落とすようにした（0.0.8）。
- **変異の範囲が設定ファイルの変更で爆発した。** `--changed` が選んだテストの coverage を
  範囲にしていたため、`package.json` / `vitest.config.ts` を触る差分では全テストが選ばれる。
  duct の実測で **全 664 テストファイル・7230 テストが走り、変異対象が 520 ファイル**
  （teps は 135 ファイルで 47 分）。導入 PR 自身が必ず設定ファイルを触るので、
  種を置くための最初の `pr` が最も回らなかった。「変更されたソース ∪
  変更されたテストが覆うソース」に変えて、上限を差分が決める形にした（0.0.9）。
- **既存の `ci.yml` が壊れた。** `@tepshq/gauntlet` は private なので、gauntlet に依存した
  瞬間、認証を持たない他の workflow の `npm ci` が 401 で落ちる。`init` は自分の workflow に
  認証を書くだけで、他の workflow のことを何も言っていなかった。
- **`init` が CI workflow を配るのをやめた（0.0.10）。** 生成された workflow は既存 CI と
  重複した上、Postgres も migrate も seed も無く、手で足したそれは `init` の再実行で消える。
  CI が要るものは gauntlet からは見えない。既に動いている job に 1 行足す形を基本にし、
  雛形は skill が持つ。今日踏んだ CI 側の問題 5 件のうち 4 件がこの形なら起きなかった。
- **mutation が判定を出せなかった（0.0.11）。** 30 行の `.tsx` に 2 分 07 秒かけて
  11 変異中 8 件が timeout、`.ts` は dry run が落ちて 1 件も走らなかった。原因は 2 つ:
  退避先の `.stryker-tmp/` が**リポジトリ内**なので duct の vitest がそのコピーを
  テストとして拾い（22 件の照合エラー、DB 行の二重書き込み）、加えて static 変異が
  変異ごとに全スイート（55 秒）を走らせていた。退避先を外に出し、`--ignoreStatic` を入れた。

  | | 元 | 退避先を外へ | + `ignoreStatic` |
  | --- | --- | --- | --- |
  | 時間 | 127 秒 | 69 秒 | **27 秒** |
  | Timeout | 8 | 7 | **0** |
  | 判定が出た変異 | 3 | 4 | **1** |

  `--ignoreStatic` は取引で、timeout していなかった判定（Killed 2・Survived 1）も失う。
  失った件数は出力に出す。副作用として、`describe` 直下で値を作るテストが変異を
  殺せなくなった（以前は静的変異として全テストが変異込みで走るので偶然殺せていた）。
  gauntlet 自身のテスト 2 か所がこれに該当し、`it` の中に移した。
- **Node 22 未満で読めないエラーになった（0.0.10）。** `node:fs` の `globSync` を使うので
  22 以上が要る。`engines` は宣言してあるが npm は警告だけで通し、実行時にモジュール解決で
  `does not provide an export named 'globSync'` だけを出して落ちる。入口を薄い層に分けて、
  中身を読み込む前にバージョンを見る。Node 20 の docker で文言と exit 2 を実測して確認した。

duct 側で片付いたもの（#563 上で解消。いずれもガイド／skill の穴として gauntlet 側に還元済み）:

- ~~CI の `npm ci` が 403~~ — Manage Actions access に `tepshq/duct` を Read で追加した
- ~~workflow に Postgres が無い~~ — `ci.yml` と同じ service container・migrate・seed を足した
- ~~`jscpd` / `dependency-cruiser` / `"gauntlet"` キー~~ — 削除した（中止した旧版の名残だった）
- ~~baseline が履歴に無い~~ — 手元で `pr` を回して種を置き、コミットした（crap 761）

### hono（パブリックリポジトリでの再生実験、2026-08-08）

honojs/hono（★31k、vitest 4、単一パッケージ、テスト 4795 件・src 177 ファイル）に
ローカルの scratch clone で導入し、**マージ済み PR を merge-base に再生**して
「エージェントがターンを終えた直後」と同じ形（未コミットのソース差分）を作った。
gauntlet は設計通り `origin/main` を優先するので、再生では起点用のローカル ref
（`replay/base` のような `/` 入りの名前）を `defaultBranch` に指す必要がある。

| 実行 | 結果 | 時間 |
| --- | --- | --- |
| `turn`（設定のみの差分、全 4795 テスト選択） | pass 触った関数 0 | 8.1 秒 |
| `turn`（PR #5196 = etag 修正の実差分） | **fail CRAP 12.0** | **2.2 秒**（テスト 17 件） |
| `pr`（同差分。フル + lint + mutation） | fail（CRAP + 種置き） | **257 秒**（うち mutation 245 秒） |

**ゲートが実際にソースを検査した初の実行。** そして最初の実弾が
「CRAP ≤ 8 の厳しさ」（DESIGN §7-4）の実データになった:
hono のメンテナが普通にマージした etag の修正が、**網羅率 100% でも CC 12 で落ちる**。
リポジトリ全体では CRAP > 8 が **86 関数 / 1146（7.5%）**。hue の 1/406（0.2%）と桁が違う。
テストが良質でも CC > 8 の関数を触った瞬間にリファクタリングを要求される、が
このルールの実際の重さ。

mutation の実測も設計の想定を 1 つ超えた: 変更されたテストが etag の 1 本でも、
そのテストは `new Hono()` でフレームワーク全体を通すので、「変更されたテストが覆うソース」が
**27 ファイルに広がって 245 秒**（CI 換算 25 分規模）。単体テストでなく統合寄りのテストを
持つリポジトリでは、テスト 1 本の変更が変異対象を大きく引き延ばす。
生き残りの種は 230 件（url.ts 42 / buffer.ts 35 / reg-exp-router/node.ts 26 …）
— よく整備されたリポジトリでも assert の弱さは普通に残っている。

baseline をコミットして同じ差分で `pr` をもう一度回すと（260 秒）、**mutation の判定が
run 間でブレた**: 差分が触れていない `smart-router/router.ts` の生き残りが 9 → 10 になり、
ratchet が落ちた。同じコード・同じテストで 2 回走らせて答えが変わるのは flaky そのもの。
Stryker の verdict は並列実行下の timeout 揺れで Survived に流れうる。
「変更されたテストが覆うソース」が変異対象を広げるほど、触っていないファイルの
ブレを踏む面積が増える。→「一巡したら決めたいこと」に追加。

hono で見つけて直したもの（0.0.13 に入った）:

- **fault 判定が「触った関数 0 の turn」を誤検知していた。** vitest は `--changed` のとき
  coverage を変更ファイルだけに絞るので、設定だけの差分では「全テスト走行・coverage 空」が
  正常。それを「噛み合っていない」と誤認して落ちた（4795 テストで実測）。
  coverage を期待できる実行（`pr` は常に、`turn` は触った関数 > 0）でだけ当てる形に直した。
- **lint の scope が「エラーのあったファイル数」を言っていた。** hono は eslint エラー 0 なので
  「lint 対象 0 ファイル」になり、309 ファイル見たのに何も見ていないのと区別できなかった。
  eslint が報告したファイル数を言う形に直した。

duct 側に残っているもの（duct の判断待ち）:

- **Prisma client が schema とずれる**ことがある（`postinstall` の `prisma generate` が
  `DATABASE_URL` 無しで失敗するため）。gauntlet の問題ではない。
- **`ci.yml` と `gauntlet.yml` が lint / 型チェック / テストで重複している。** `ci.yml` が
  Node 20 なので 1 本にまとめられない。CI の Node を上げるかは duct の判断（#563 に記載）。
- **CI の `pr` が 13 分**（うちテスト 11.5 分。手元の 7 倍）。`gauntlet.yml` に timeout は無いが、
  テストが増えれば `ci.yml` の 15 分感覚を超えていく。

---

## 前提が崩れたら戻る先

| 起きたこと | 戻る判断 |
| --- | --- |
| TypeScript の下限要求で実プロジェクトの型チェックが通らない | 下限の設定（DESIGN §5） |
| `oxc-parser` が実コードをパースできない | パーサの選定（DESIGN §2） |
| `oxc-parser` の 0.x 更新で CC の値が動いた | パーサのバージョン固定方針（DESIGN §2） |
| CRAP ≤ 8 が dogfooding で機能しない厳しさだった | 閾値 8（DESIGN §3） |
| `turn` が数秒に収まらない | マージ方式（DESIGN §2）→ 最悪 CRAP を `pr` に移す |
| マージ済み coverage の精度が実用に足りない | マージ方式（DESIGN §2） |
| baseline の後退経路がまだ残っていた | ガードの網羅性（DESIGN §3） |
